# Threads table as it stood on 2026-09-10

The `## Threads` rows of `README.md` before they were condensed to one line each. Verbatim, in the
original order. The live router is the README; this is the record of what each row said when it
was trimmed.

| Thread | State | Next |
|---|---|---|
| `native-aa-recovery-identity-and-speed` | **Nothing queued.** **Round 4 done (`289595df`, transfer `native-aa-recovery-identity-and-speed-round4-results.md`) — no FAIL, both round-3 findings fixed, PR-ready.** Gate 1489/0 (`DriverCandidatePolicyTest` 19, `NativeDriverSelectionPolicyTest` 71), candidate md5 `eeebeb94…`, baseline = round 3's candidate APK `bdf1cccd…`. Tie-break works: D-MOTO `FX Plus`/`KY Pro` (both device class `0x0404`) and D-POCO `Navegadortz2` (`0x0408`) now read `NOT_A_PHONE, advertises the Audio Gateway record but device class 0x…​ is not a phone`; **every roll-up on both head units reads `1 phone`** (D-MOTO `1 phone, 0 unknown, 8 not a phone`, D-POCO `1 phone, 0 unknown, 3 not a phone`). Selector crash guard proven by an on-demand A/B (R2b): baseline still `FATAL EXCEPTION … not attached to a context` at `HomeFragment.kt:1020` with a new PID; candidate survives the same orientation relaunch (no FATAL, same PID, `finishDrawing of relaunch` present) and the replacement fragment auto-connects. R2a startup connects to POCO X3 NFC only, PID stable. R3 selector: `sel1` 1 phone row + toggle `Show all Bluetooth devices (8)`, after one tap `Show only phones` + 9 rows. R4a/R4b poke only the driver phone (`8`/`3` not-poked). Connect times back to round-2 clean: **D-MOTO 11.4 s, D-POCO 7.9 s** launch→SSL (round 3 was ~101 s / ~35 s). Caveats, all rig not code: (1) `Navegadortz2` is now *persistently* absent from the moto's `adapter.bondedDevices` (0 of 7 D-MOTO launches), so D-MOTO counts run −1 vs the brief and its verdict is confirmed on D-POCO instead; (2) R3 unreachable "with the app already running" — 1 classified phone auto-connects and the projection covers the selector; reached via `headunit://disconnect` then a warm `show_driver_selector` deep link; (3) R5b's first D-POCO bring-up formed no session because the moto driver phone kept WiFi-Direct group-owner state from the D-MOTO block — cleared with a WiFi cycle, retried clean. New scripts `native_aa_recovery_r4.sh`, `native_aa_recovery_r4_r3sel.sh`. **Post-round addendum in the results file:** the operator paired a real 2nd phone (`Galaxy S24+`) to D-POCO and the pre-round build `959467e2` (the restore) crashes on every launch — the standing selector crash (`HomeFragment.kt:1023` / `showNativeAaDeviceSelector$lambda$54` → `onShow` → `requireContext()`), reached by a genuine 2-phone bond rather than the R2b deep link. Candidate `289595df`: 0 crashes, `2 phone` roll-up correct, session forms — so the branch fixes it. D-POCO left on the candidate. **Round 3 was — R1 FAIL on both head units, round stopped after R1a/R1b/R2. Results `native-aa-recovery-identity-and-speed-round3-results.md`.** R0 gate 1487/0 clean (candidate md5 `bdf1cccd…`). Candidate `545595d6` on `12706e26`; baseline `7d1f93f5` (round 2 APK `96c326cc…`). The commit classifies driver candidates by SDP role; on this fleet **three bonded non-phones advertise an HFP/HSP Audio Gateway record** (`Navegadortz2` — its own class is `0x0408` hands-free —, `FX Plus`, `KY Pro`), so "role beats class" calls them PHONE: D-MOTO roll-up `4 phone` (want 1), D-POCO `2 phone` (want 1). Mixed vs baseline: candidate correctly hides the Redmi Watch 5, the AA/CarPlay dongle and `BC8-Android` that baseline drives, but newly promotes the three AG-record devices baseline left alone — needs a tie-break (AG record + non-phone class ⇒ not a phone). Separately, a **standing crash** (not this branch): `HomeFragment.showNativeAaDeviceSelector` `setOnShowListener`→`requireContext()` ISE fires on every launch on both phones once >1 phone is bonded (candidate 3/3, baseline 1/1) — the selector UI is unusable for this branch's target population. R2 INCONCLUSIVE (both arms crash before connecting; sighting unreproducible on the rig). R3/R4/R5/R6 not run — premise ("exactly one phone, selector never shows") invalidated. Sessions still formed headless via the surviving service, 35–101 s (poking non-phones first). **Round 2 done, no FAIL.** Candidate `7d1f93f5` (the brief's `583729a6` plus one settings-UI commit), baseline `b5617bd5`; 1466/0 and 1450/0. **R0-R4 PASS** on the MT50 (creating->SSL ~9-11 s, 3/4 sessions, the miss a phone-side association stall with every head-unit step correct; all six new zero-count lines 0). **R10 PASS on the candidate**: one early wake, one credential-less `Handshake failed` at 60.8 s, the stop line 1 ms later, zero pokes for the rest of the WiFi-off window, a session 10 s after WiFi returned. Its baseline half is INCONCLUSIVE, and the results file's reason for that is wrong: `97430ddf` does not touch the poke loop. Both arms went quiet because the phone dialled back and a handshake in flight stops the loop (`NativeHandoffPolicy.loopStep`), so the half-minute re-wake the brief predicted is unmeasured on any arm. **R7 not exercised** again (no accepted-and-hanging create on either unit); `74eb8414` stays JVM-covered. **R11 PASS**: D-POCO head unit / D-MOTO phone, one 5 GHz group, SSL at 11.7 s, no SoftAP with `native-ap-transport`=0. Still hardware-unmeasured: the create wedge and the below-API-29 profile purge, both waiting on reporter builds. Results: `native-aa-recovery-identity-and-speed-round2-results.md`, `native-aa-recovery-identity-and-speed-round1-results.md`. |
| `pr954-review` | **answered, coding session, no rig time; nothing queued** | PC-only review of [PR #954](https://github.com/andreknieriem/open-headunit/pull/954) (Sesam17, head `88f78f4f`, base `main` `12706e26f`), the small-branch follow-up to #943. `pr954-review-findings.md` has the evidence, `pr954-review-reply-draft.md` the unposted reply. Verdicts: the exit-action commit (`e0e192f`) is a good idea with a blocking bug (its `App Home` branch loops through `MainActivity.bringProjectionToFront`) plus a default flip against the zero-value rule, and it duplicates `moveToBackground()`; the flow-control commit (`925d55d`, video x4 / audio x2 on the Ultrawide toggle) is a reject: it overflows the 50-chunk audio queue the 30-message window was sized against, dilutes `4e7f3b740`'s pacing brake, gates on a `lateinit` read from the poll thread, and does not compile once PR #948 lands. Nothing posted to GitHub. |
| `ultrawide-touch-alignment (round 8)` | **Round 8 done — no FAIL, and R6 proves the hash commit on hardware.** Built from **`fork/testing/recovery-plus-video-plus-usb` @ `694af3e1a`** rather than the fix branch (session instruction); it merges `fix/video-fit-and-ultrawide-touch` @ `89093db4` at `2e35411ad`, so both screen-config commits are in the tree unmodified on top of the native-AA recovery and USB stacks. Candidate md5 `8da1475b`, baseline `409056f4` rebuilt as `e9581d3a`, R6 arm `c70ed1b40` (= testing tip with `89093db4` reverted) `9cadf6f9`; identity carried by the symbol matrix, which separates all three. Results `ultrawide-touch-alignment-round8-results.md`; images in `ultrawide-touch-alignment-round8-photos/`. **R0 PASS: 1592 tests / 0 failures** — not the brief's 1499, because the testing branch carries ~93 tests the fix branch does not; the four brief-named classes are exact (`ScreenOrientationPolicyTest` 6, `ScreenSettingsHashTest` 5, `ProjectionCanvasPolicyTest` 14, `TouchCoordinateMapperTest` 7). **R1 PASS (decisive):** on the candidate `usable=1168x720` and `shape=MARGIN` appear **zero times**; final anchor `1440x720` == `mBounds` and steady `usable 1304x720` == `mAppBounds`, both to **0 px**; `PAR 10000`, one `Honest Init`, exactly **3** `measured on the activity window, against the insets now in force` lines spanning **289 ms**. The baseline arm reproduces round 7's signature: the double-shrink for **131 ms** (round 7 said 140), corrected by the projection surface **67 ms before `makeProto`**, with `canvas moved from the announced` latching and no retraction line existing on that build. **R2 / R3 PASS**, bit-identical to round 7 R2 / R4 on all seven and all six figures; the re-derivation line that fires 3× in STATUS_ONLY fires **0×** in both, because neither mode has an inset in force. **R4 PASS on all three conditions, with the caveat the brief asked for:** after `pm clear` the first `[RES_CAP]` reads `usable=1304x720`, **0 px** from the content area and not the 136-short `1168`, `PAR 10000` — but the first `Honest Init` says `(from WINDOW)` and `(from DISPLAY)` appears **0 times**, so a MainActivity layout beat the display fallback and **the fresh-install last-resort path was never reached on this rig**; it stays JVM-only coverage. The wizard wrote nothing (`resolutionId` still 2 after the run). **R7 PASS (D-HU touch in STATUS_ONLY, never run before):** all 10 `Touch map` lines read `view=1304x720` and no other value, X exactly proportional with no offset and no clamp (`496→487`, `704→691`, `1284→1260`), both rail taps opened their control, corroborated phone-side by `GhFacetBar injectMotionEvent x=487.0 / x=691.0` matching the mapped `video=` to the pixel — the ~88 px shift the brief warned about did not occur. **R5 PASS (D-MOTO Self Mode Immersive):** one `Honest Init` `(from WINDOW)`, `(from DISPLAY)` 0, no portrait reading adopted (`anchor -> 1080x2400` 0, `chosen=_1080x1920` 0 — the three `1080x2400` mentions are the pre-normalisation `Raw size:` diagnostic and `onSurfaceChanged`, normalised in the same millisecond), `margins drifted` 0 and `UpdateUiConfigRequest` 0 against round 7's two of each. **But condition 4 is unexercised, not earned:** §7 specifies `video-fit-mode=0` (FILL), which on this 2.222-aspect panel is `MarginStrategyPolicy`'s PAR path with **no margin at all** (settled `Margins 0x0` / `1.0,1.0` / `PAR 12500`), so the drift machinery was never engaged and round 7's `0x216` / `scaleY 1.25` comparison target does not exist under this brief's own settings — that was a CONTAIN run. **R6 PASS and it is the round's strongest result:** the reverted arm reproduces round 7's finding exactly, `Settings changed (-1320151468 -> -1320149578)` — **delta +1890, the precise figure the brief derived** — then `Honest Init | Anchor: 2300x1017 (from DISPLAY)`, while the candidate shows 1 init, 0 `(from DISPLAY)`, 0 `Settings changed`, 0 `canvas moved`. **`89093db4` is proven on this rig.** Two things the A/B shows in passing: the `canvas moved` / `canvas returned` pair retracts in **11 ms** on both builds carrying `94efd6df` (the round 7 latch is gone independently of the hash commit), and `94efd6df`'s re-derivation corrects the bad reading 11 ms later so even the reverted arm never announces `2300x1017` — the two commits are complementary, and the missing hash fix costs a spurious init, a spurious unlock and an 11 ms wrong canvas rather than a wrong wire announcement. **Setup:** added `hur-wifi-test-scripts/seed_hu_settings_fresh.sh` (seeds `settings.xml` from nothing after `pm clear`, which `r6_set_hu_prefs.sh` cannot do); note `restorecon` leaves the new file without the app's SELinux categories and an explicit `chcon` is needed. **Erratum in this brief: §7's precondition check reads the wrong table** — Gearhead binds the IPv6 wildcard, so `/proc/net/tcp | grep :149D` is empty while the head unit server is running; it is in `/proc/net/tcp6`. The round held for an operator escalation on that check. Other deviations: R4 needed `wifi-connection-mode=3` and `view-mode=2`, which §2 omits and `pm clear` wipes (without them the rig has no transport); R7's rail is horizontal on D-HU so the two rail taps are its ends, not "top-most"/"bottom-most"; no `Frames: frame=` line exists in this ROM's window dump, so anchors are scored against `mBounds` / `mAppBounds`. Settings restored md5-identical on D-HU (`06fb1e94`) and D-MOTO (`007bead8`); D-HU and D-MOTO both left carrying the candidate, D-POCO untouched. **Next: nothing queued.** Two things for the coding session: the `MarginStrategyPolicy` FILL→PAR clamp (R5, still open from round 6 R4 and round 7), and whether the fresh-install display fallback deserves a hardware run on a unit whose surface arrives after service discovery, since this rig cannot reach it. Brief: `ultrawide-touch-alignment-round8-brief.md`. |
| `ultrawide-touch-alignment (round 7)` | **Round 7 done — the measured-canvas commits are hardware-clear on D-HU in all four screen modes, and they close the D-MOTO gap the round 6 review had written off as unfixable on the wire.** Candidate `fix/video-fit-and-ultrawide-touch` @ `409056f4c`, APK md5 `32b52a15`; baseline `e4be4e0e3` **rebuilt this round** (`6abf549a`) because all three devices were carrying another thread's build, not round 6's `959467e2` — identity carried by symbol (`ProjectionCanvasPolicy` 9/0, `noteWindowContent` 1/0, and `use_measured_touch_surface` 0 on the candidate / 3 on the baseline). Results `ultrawide-touch-alignment-round7-results.md`; images in `ultrawide-touch-alignment-round7-photos/`. **R0 PASS:** **1482 tests / 0 failures**, exactly the gate; `ProjectionCanvasPolicyTest` 8/8, `TouchCoordinateMapperTest` 7/7. **R2 PASS (decisive):** D-HU Screen mode Normal announces `Anchor: 1304x720 (from WINDOW)` on **one** `init`, equal to the drawn content area to **0 px**, `PAR 10000` by the 3% dead band (`1304/1280 = 10188`, 1.88% from square), `Margins 0x0`, `Normal Scale 1.0/1.0`, `shape=PAR`. **R1 (measurement):** the baseline needs **two** inits — `Anchor: 1440x720` first, then `1304x720` off `Using cached surface dimensions` 414 ms later, landing **18 ms** before `makeProto`. The reporter's 8:3 fault does not reproduce here: D-HU's baseline wins that race, so this rig can prove the new path right but not the old path wrong. **R3 PASS:** touch A/B byte-identical on both arms — `view=1304x720` in every `Touch map` line (the content area, not the 1440 display), no clamp (bottom tap `video=44,671`), both rail taps open the control aimed at, and Gearhead's `injectMotionEvent` receives exactly the mapped coordinate. **R4 PASS:** Immersive bit-identical to round 6 R1 (`1280x720` / `0x0` / `PAR 11250` / `1.0,1.0`); first attempt discarded on 2 `createGroup SUCCESS`, re-run clean. **R5a PASS + finding** (STATUS_ONLY, never measured here): final anchor `1440x720` == window frame, `usable=1304x720`, `PAR 10000` — but for **140 ms** the canvas is shrunk twice (`realScreen=1304x720 usable=1168x720`, `shape=MARGIN, margins: w=112`) before the surface reading corrects it; it never reaches the wire on this rig, and the new `canvas moved from the announced` instrument fires on a **transient** inset flap that settles back to the announced value. **R5b PASS** (IMMERSIVE_WITH_NOTCH): `Anchor 1440x720 (from WINDOW)`, `usable=1440x720`, `PAR 11250`, full-bleed, one init, no drift line — the cleanest of the four modes. **R6 PASS:** all three connects announce `PAR 10000`; connect 3 (after `am force-stop`) reads `(from WINDOW)`, never `DISPLAY`, and connect 2 (in-process) does not re-init at all. The live screen-mode change is **INCONCLUSIVE on hardware and belongs on the JVM**: rewriting `fullscreen-mode` under a running app and `am start`-ing MainActivity is inert (`Activity not started, intent has been delivered to currently running top-most instance`; prefs are cached in the process), the session survived untouched and `canvas moved` never printed — applying a mode change here needs a force-stop, which restarts the session by definition. **R7 PASS + finding (D-MOTO, CONTAIN):** **both rail taps hit their control**, contradicting the review's prediction, because `anchor 2300x1017 -> 2400x1080, measured on the activity window` pulls `scaleY` back from `1.2720848` to `1.25` and the margin back from the settled `0x231` to the **announced `0x216`** — the fix does not re-announce, it stops the anchor being wrong, so the inert `UpdateUiConfigRequest` stops mattering. Its own finding: the second `Honest Init` in that session reads **`(from DISPLAY)` inside Immersive** at `2300x1017` (corrected 17 ms later by the window), and D-MOTO's first window reading is the portrait `1080x2400`, adopted for 61 ms — the live window reading has no orientation guard where `cachedMeasurement` does. **R8 PASS (answered):** `Cache invalidated (hash mismatch` then `Using cached surface dimensions`, exactly once each per **baseline** connect and **zero** across all seven candidate captures; the stored hash never matches on a fresh process, the review's code reading confirmed on hardware. Round 6's raw D-MOTO captures no longer exist on the rig or this branch, so that half was answered from this round's own baseline captures instead. **Setup:** added `hur-wifi-test-scripts/ultrawide_touch_r7.sh` (three-phase `connect`/`taps`/`end`, so tap targets can be read off the live arm and the framework window frame captured while the projection is up). Erratum: `ultrawide_touch_r3.sh` waits for `Handshake: SSL handshake complete`, which is a **DEBUG** line — at `log-level=2` it never matches a healthy session (the INFO line is `AapSslContext.performHandshake | SSL handshake complete`); round 6's "2 per session" SSL count is a VERBOSE artifact. Tap 5 used `1284,360`, not the brief's `1420,360`, which is the OEM bar's Home key. R7 needed **one operator tap** (Gearhead 17.4+ Self Mode needs AA's developer head-unit server on `:5277`; no adb route) — the round's only escalation. Settings restored md5-identical on D-HU (`06fb1e94`) and D-MOTO (`6de14644`); both carry the candidate APK. **Next: nothing queued.** Two things for the coding session before the branch lands: the STATUS_ONLY double-shrink window (R5a) and the two D-MOTO ranking gaps (R7). Brief: `ultrawide-touch-alignment-round7-brief.md`. |
| `ultrawide-touch-alignment (round 6)` | **Round 6 done — the #809 squash is closed on the rig and the round 4 R5 touch regression is fixed.** Candidate `fix/video-fit-and-ultrawide-touch` @ `e4be4e0e3` (`main` `12706e26` + 3 commits), APK md5 `959467e2` / baseline `aac9797f` (both versionCode 106). Results `ultrawide-touch-alignment-round6-results.md`; photos in `ultrawide-touch-alignment-round6-photos/`. **R0 PASS:** candidate builds clean, **1473 tests / 0 fail** (baseline 1414/0); all 6 new policy test classes green. **R1 PASS:** D-HU FILL AUTO bit-identical to round 5 R0 (`1280x720` / `0x0` / `PAR 11250` / `1.0,1.0`). **R2 PASS (decisive):** D-HU forced 1080p — candidate `1920x1080` / **`0x0`** / **`PAR 11250`** / **`1.0,1.0`** (`hardCeiling` did not cap); baseline `1920x1080` / `480x360` / `scaleX 1.333 scaleY 1.5`, rail icons and speed-dial visibly vertical ovals (`shape_compare.jpg`). **R3 PASS:** D-POCO touch A/B — candidate `Touch map` is `video_y = raw_y/panelH·uiH` with **no phantom centring offset** (round 4 R5's `+72` is gone); tapping the visible centre of Maps / Spotify / Phone opens each correctly; AA receives the PAR-composed coordinate (panel `x=95` → AA `x=95`). **R4 PASS + finding:** D-POCO 1080p FILL now announces **`PAR 12500` / `Margins 0x0` / `1.0,1.0`**, not round 3-5's `Margins 0x216` / `scaleY 1.25` — `MarginStrategyPolicy` flips *any* FILL panel wider than 16:9 to PAR, so the 2.222-aspect rig phone is now the common PAR case, not an edge case. Picture + touch correct (1:1), arguably better (full buffer, no blanked rows), but rounds 3-5's D-POCO margin baseline no longer exists. **R5 PASS:** CONTAIN/COVER plumbed end to end — D-HU 720p CONTAIN `scaleX 0.8889` pillarbox with edge-clamped touch (`raw=20 -> video=0`), COVER `scaleY 1.125` fills width + crops 45 px top/bottom with crop-compensated touch; both keep `PAR 10000` / `shape=MARGIN`, neither distorts. Incidental: AUTO+CONTAIN/COVER on D-HU negotiates `1920x1080` (autoLadder counts the long side for non-FILL). **R6 PASS:** `stretch_to_fill`→`videoFitMode` migration, all 4 cases (`true`→FILL, `false`→CONTAIN, neither→FILL, `false`+`forced_scale`+SURFACE→FILL inverted). **R7 INCONCLUSIVE at report time** (D-MOTO off the rig) — **now confirmed PASS**, see the D-MOTO addendum below. **R8 PASS:** floating button defaults off, no overlay-permission redirect on launch. **Setup:** added `hur-wifi-test-scripts/r6_set_hu_prefs.sh` (multi-key rooted pref writer, no relaunch); `ultrawide_touch_r3.sh` reused unchanged. Discard rule clean every run (1 `createGroup`, 0 `Magic Garbage`). Settings restored md5-identical (D-HU `b3769c5a`, D-POCO `fcdd883d`); both carry the candidate APK, D-MOTO untouched. Brief: `ultrawide-touch-alignment-round6-brief.md`.<br><br>**D-MOTO addendum** (`ultrawide-touch-alignment-round6-addendum-dmoto.md`, run on request — the round's reporter saw misalignment on D-MOTO, a Moto edge 30 neo in Self Mode): **the misalignment is the round 4 R5 centring regression, still live on the build D-MOTO was carrying (`3c57e530`)** — it maps FILL touch as `video_y = margin/2 + proportional`, so on its `0x231` margin every vertical tap lands ~115 buffer-rows low (near the top: `raw=1200,60 → video y=166` where it should be `64`). **The round 6 candidate fixes it** in both FILL (`→ y=64`) and CONTAIN (`→ y=50`) — top-left anchor, no offset. **R7 confirmed PASS on D-MOTO:** the connect-time inset settle moves the margin `0x153 → 0x231` after `makeProto`, `onMarginsDiverged` fires, `reannounceMargins` sends the corrected per-side margins (`T=115 B=116`) **exactly once**. **New finding:** on the FILL/PAR path the candidate announces `PAR 11651` derived from the *pre-settle* panel reading and **never re-announces it** — the drift check only watches the margin, which is always 0 under PAR. ~3-8% aspect error on this rig (not eye-visible), but the re-announce logic needs to cover the PAR the way it covers the margin. **Next:** branch is PR-ready pending (a) extend drift re-announce to the PAR (D-MOTO addendum), (b) a look at `MarginStrategyPolicy`'s clamp/dead-band now that PAR is the common path on 2.2-aspect panels, (c) Sesam17's own-build test on his 8:3 unit (brief §10). D-MOTO now carries the candidate APK.<br><br>**Review notes** (`ultrawide-touch-alignment-round6-review.md`): R4's finding is the design and was predicted; the D-MOTO settled ratio is 12721 (residual 7-9%, not 3-8%); `UiConfig` has no ratio field and `UpdateUiConfigRequest` is measured inert, so R7 is a send count and (a) above is not a fix; the D-MOTO CONTAIN pass needs a control-hit check. **Round 7 queued on D-MOTO, three asks in the review file:** the cache grep, two more FILL connects (in-process and after a force-stop), and a CONTAIN bottom-rail tap read from which app opened. |
| `ultrawide-touch-alignment (round 4)` | **Round 4 done — R5 FAIL; the branch does not clear this round.** Candidate `fix/video-fit-and-ultrawide-touch` @ `2a8ce83f`, baseline `main` `12706e26`. Results `ultrawide-touch-alignment-round4-results.md`; photos in `ultrawide-touch-alignment-round4-photos/`. **R0 gate PASS** (candidate 1458/0, baseline 1414/0, md5s differ). **R1 PASS** — D-HU AUTO candidate now negotiates `_1280x720` / `Margins 0x0` / `PAR 11250` / `scaleX 1.0 scaleY 1.0`, 5 taps byte-exact to round 3's R1; photo full-bleed undistorted. **R2 (measurement)** — D-HU AUTO baseline negotiates `_1920x1080` / `Margins 480x360` / `1.3333334/1.5`, and its picture is also full-bleed and undistorted to the eye on this near-square (PAR ~1.125) panel: the ladder change buys bandwidth here, not visible quality; the visible win needs a panel far from square, which this rig lacks. **R3 PASS** — D-HU manual 720p byte-identical to round 3's R1. **R4 INCONCLUSIVE + finding** — the candidate's new panel ceiling caps **manual** 1080p to 720p on a 720-row panel (`resolutionId=3 -> capped=_1280x720 changed=true`; round 3's candidate did not cap manual picks), so no margin is ever produced on D-HU and the margin-present touch path is unreachable here. **R5 FAIL (decisive)** — D-POCO A/B, verified against Android Auto's own `GhFacetBar injectMotionEvent` coordinates (synthetic `input tap`, not physical touch): baseline `95,625` -> AA `(50,333)` -> Spotify facet; candidate same tap -> `Touch map raw=95,625 -> video=51,405` -> AA `(51,405)` -> **AssistantScrim**, the control one rail-slot below the target. Candidate needs the tap moved **135 px up** (`95,490 -> video 51,333`) to hit Spotify -- exactly half the announced 144-row margin. **Android Auto anchors its canvas at the buffer top-left; the old (baseline) mapper was right**, and the canvas-centring change in `2a8ce83f` (`TouchCoordinateMapper` / `AapProjectionActivity`) displaces every touch on a margin-present panel downward by half the margin. Per the brief the fix belongs in the renderer, or the centring offset is dropped. R6/R7 not run (brief: stop D-POCO runs on an R5 FAIL). **R8 INCONCLUSIVE (rig)** — geometry matches D-POCO / round 3, but the tap A/B could not run: the Moto's digitizer sprayed continuous phantom touches (operator confirmed both phones' physical touch is miscalibrated -- irrelevant to R5's synthetic method). **R9 matches brief** — D-HU resolution picker now reads **"720p (Recommended)"** where `main` says 1080p; DPI-screen over-panel warning cites 720p; runtime cap, wizard and DPI all agree via `SystemOptimizer.panelCeiling`. **Setup:** no scripts added (`set_hu_prefs.sh` / `set_prefs_runas.sh` / `ultrawide_touch_r3.sh` / `thermal_guarded.sh` reused). Settings restored md5-identical on all three devices; candidate APK left installed on all three. **Next: the touch-centring offset needs rework (drop it, or move it to the renderer) before another round; the ladder / PAR / video-fit half is sound. Round 5 (buildless PAR/margin probe) is queued above and orthogonal to the R5 FAIL.** |
| `ultrawide-touch-alignment (round 3)` | **Round 3 done — R0-R9 all PASS, on `fix/video-fit-and-ultrawide-touch` @ `0b6497b41` (2 commits on `main` `6cd70399`).** Results `ultrawide-touch-alignment-round3-results.md`; photos in `ultrawide-touch-alignment-round3-photos/`. **R0 gate PASS**: candidate 1453/0, baseline 1414/0, exact. **MT50 (Native AA), candidate:** R1 720p `scaleX 1.0/scaleY 1.0`, `Margins 0x0`, `PAR 11250`, 5 taps on round 1's table exactly; R2 1080p `scaleX 1.3333334/scaleY 1.5`, `Margins 480x360`, `PAR 10000` bit-identical to round 1 R4; **R3 720p CONTAIN `scaleX 0.8888889/scaleY 1.0` and `PAR 10000`** (round 1 would have said 11250) — photo shows a clean pillarbox, no horizontal squeeze. **POCO X3 NFC (Self Mode), 2400x1080:** R4 baseline 1080p measured `Margins 0x216`/`scaleY 1.25` (reference photo, full-bleed undistorted); **R5 candidate 720p FILL `scaleY 1.25`** (round 2 measured `1.0` and wrongly passed it), `Margins 0x144`, `PAR 10000`, taps identical to round 2 R3, photo matches R4's proportions with no vertical squash; R6 candidate 1080p `scaleX 1.0/scaleY 1.25` identical to R4 baseline (1080p path unchanged by the fix); **R7 CONTAIN + COVER both collapse onto FILL** — identical scale, identical taps, and the photos show no head-unit letterbox and no crop (the dark left strip is AA's own control rail, present in FILL too), so this is the fix, not round 2's Finding 1. **Moto edge 30 neo (Self Mode):** R8 R5/R7 abbreviated — `scaleX 1.0/scaleY 1.25`, `PAR 10000`, confirming tap identical to the POCO in FILL/CONTAIN/COVER, cross-device match (one Self-Mode session dropped `link_lost` and was re-run clean). **R9 PASS**: the Moto's connect-time inset settling reliably produces `CarScreen: margins drifted from the announced 0x102 to 0x154 to 0x144`, and **every drift line is followed by `[UI_DEBUG_FIX] … send corrected via sendUpdateUiConfigRequest`** — round 2's Finding 2 fixed and reaching the re-announce path; the deliberate immersive-mode provocation was inert (surface already fullscreen). **Setup:** added `hur-wifi-test-scripts/thermal_guarded.sh` (SIGSTOP/SIGCONT a build tree on CPU temp — the baseline assemble peaked 95 °C before it was added) and `ultrawide_touch_r3.sh` (the round driver). Settings restored on all three devices; candidate APK left installed on all three. **Nothing queued next — the branch is PR-ready per this round + the findings doc.** |
| `ultrawide-touch-alignment (round 2)` | **Round 2 done on real phone panels via Self Mode (POCO X3 NFC + Moto edge 30 neo, both 2400x1080 landscape, wider than the MT50's 2.0 and past the code's "small screen" 1920x1080 threshold — R0 skipped, reused round 1's APKs), same `371477b6`/`6cd70399`.** Results `ultrawide-touch-alignment-round2-results.md`. Core fix confirmed again on both phones: baseline `scaleY: 1.25` (worse than the MT50's 1.125, matches prediction), candidate restores `1.0`/`1.0`, R3 touch alignment exact on both. **Two new findings** neither phone nor a small-screen panel could have caught: (1) **CONTAIN/COVER touch mapping ignores the real letterbox/crop when margin is non-zero** — `ProjectionViewScaler`'s visual scale uses the raw negotiated aspect while `TouchCoordinateMapper`'s own CONTAIN/COVER branch uses the margin-reduced aspect, and on any panel past the small-screen threshold those two aspects coincide by construction, so the mapper concludes no letterbox is needed while the view is genuinely pillarboxed/cropped — reproduced identically on both phones, real content misalignment, not yet fixed (flagged for before CONTAIN/COVER ship). (2) **the margin announced to the phone at connect time can differ from what the head unit later uses for touch** — Moto announced `0x102` from a transient screen reading mid-settle, then touch silently used the live `0x144` once the screen settled, no re-announcement; intermittent (didn't reproduce on POCO this session), device inset-settling race not app-controlled. Round 1's MT50 result stands; these are additional gaps. Next: root-cause finding 1 before CONTAIN/COVER ship; finding 2 routed to a JVM regression test rather than more hardware chasing. |
| `ultrawide-touch-alignment (round 1)` | **Round 1 done, all seven runs PASS, on `fix/video-fit-and-ultrawide-touch` `371477b6` (2 commits on `main` `6cd70399`).** Results `ultrawide-touch-alignment-round1-results.md`. **R0 build gate PASS**: candidate 1445/0, baseline 1414/0. **R1 baseline PASS**: `scaleY: 1.125` at 720p exactly as predicted, `NegotiatedResolution 1280x720`/`Margins 0x0`; the same arm's 1080p leg gave `scaleX: 1.3333334, scaleY: 1.5` / `Margins 480x360` for R4 to compare against. **R2 candidate PASS**: `scaleY` restored to `1.0` and `PixelAspectRatioE4 is: 11250`, both independently confirmed and neither key present as a stored override (`ACTION_GET_SETTINGS`). **R3 touch alignment (the point of the round) PASS**: all five `input tap` points landed exactly on the predicted table, y always equal to raw y, x scaled by exactly 0.8889 — the pre-fix failure mode does not occur. **R4 regression guard PASS**: 1080p unchanged, margins `480x360`, PAR `10000`, scale line bit-identical to R1's 1080p capture. **R5 CONTAIN PASS**: `scaleX: 0.8888889`, taps inside the pillarbox correctly clamp to the picture edge. **R6 COVER PASS**: `scaleY: 1.125`, every tap's y matches `(raw_y + 45) / 1.125` exactly, correctly compensating for the 45px top/bottom crop. **Rig notes:** this round resumed a build gate left mid-session by a suspected PC thermal interruption (baseline's unit-test log was truncated with no final result); temps were nominal at resume (58°C, peaked ~84°C during the candidate build, both runs completed cleanly) — see results file Setup notes. Found and worked around an `ACTION_LOG_MARKER` quoting bug in the tester's own commands (adb multi-arg rejoin swallows quotes on a multi-word `--es` value), harmless to the verdicts since `ACTION_QUERY_STATE` delimited every run instead. `settings.xml` restored, diff clean. Next: nothing queued for this thread; ready to report back to Sesam17 / land the branch per the findings file's §6. |
| `wireless-bring-up-and-5ghz` | **Round 3 done — R0/R1/R6/R7/R8 PASS, R9 0 sightings (not a FAIL), on `testing/wireless-plus-automation` `1f4b85ce` (stamp `1f4b85ce5a59`, gate 1414/0, md5 `b892fcf6cc3740bee42e254ba8277559`). `e6ed1ad2` is hardware-clear; nothing queued.** Results `wireless-bring-up-and-5ghz-round3-results.md`, evidence `evidence/wireless-bring-up-and-5ghz-round3/`. **R6 PASS, shape A (serialised), 3/3** — round 2's ~11 s two-socket overlap is gone: the manual `Calling socket.connect()` opens 0.02–0.10 s *after* the automatic one's failure line, never during it (r6b 22:27:16.789→.892, r6c 22:30:17.551→.610, r6d 22:31:30.455→.478). `another poke is already connecting …` fires exactly once per run. Delay `ACTION_NATIVE_AA_POKE`→`Attempting manual poke` = **15.20 / 15.30 / 15.30 s** vs round 2's failing 4.11 s (the wait now releases when the slot frees at ~15.4 s, short of the 20 s `Step.ABANDON` bound — shape B not reached, correct for a single ~15.4 s failing record). **R1 regression guard PASS: onCreate→`SSL handshake complete` 21.22 s** (round 2: 21.42 s), 1 group, 1 SSL, 1 `Incoming connection detected`, GLES projection up, `MATCH!`=1 benign, `awaitPokeSlot` never entered (one poke in flight → PROCEED first poll). Group freq 5180 MHz (automatic). **R7 PASS** (`Poke via HFP-AG … failed: … read ret: -1` at `I/`, `log-level=2`). **R8 PASS 3/3** (automatic poke = exactly one `Calling socket.connect()`, no HSP-AG after). **R9: 0** automatic-gate sightings and **0** `for 20000ms` abandon lines anywhere — `triggerPoke()`'s device-loop wait and the 20 s abandon stay JVM-only, unexercised on this rig. Watch item `a chosen driver's wake poke is running …` still fires after the manual poke wins (3× in r6b). **Rig notes:** POCO started wireless-adb-only (can't cut its WiFi) so R1 ran once BT-only (33.33 s, inflated by phone 5500/5785 station coexistence — the app's own `logStationCoexistence` warns of it, not an `e6ed1ad2` regression) then re-ran canonically after the operator USB-cabled it (`4f4027e9`). `WifiDirectManager: … createGroup SUCCESS!` carries a frozen `22:20:30` wall-clock stamp intermittently on this rig (correct write-order; R1-canonical stamped it right). `logcat -c` unreliable (stale buffer ahead of every R6 capture; filtered by timestamp). `r6b`'s 78 s window ran past the 60 s no-phone `recoverNativeGroup` → a benign second `createGroup`/manual-poke cycle 45 s after the decisive window; `r6c`/`r6d` used 48 s and end clean. MT50 `settings.xml` restored (only app-managed `station-stand-down-network-id` 2→-1 and GPS coords differ). New script `wireless_bringup_5ghz_r3_r1.sh` (BT-only R1 gate for a wireless-only phone). ~~round 3 queued~~ Round 2 done — R0/R1/R5/R7/R8 PASS, R6 FAIL, on `5753dfd9`, results `wireless-bring-up-and-5ghz-round2-results.md`, evidence `evidence/wireless-bring-up-and-5ghz-round2/`. **R1 regression guard PASS:** onCreate→`SSL handshake complete` **21.4 s** (round 1: 21.2 s), 1 group, 1 SSL, `MATCH! Starting AapService`=1 benign, no churn (a stale `p2p-wlan0-10` group left from R6d was removed at launch, then `-11` created — one formation). **R7 PASS:** `NativeAA: Poke via HFP-AG to POCO X3 NFC (DC:B7:2E:5E:4E:59) failed: read failed, socket might closed or timeout, read ret: -1` at `I/` with `log-level=2`. **R8 PASS:** the cancelled automatic poke logs exactly one `Calling socket.connect()` and no `HSP-AG` after it (the `ensureActive()` check took), 3/3. **R5 PASS (backstop path):** the new `AapService: the USB attempt did not settle within 8475ms — arming wireless anyway` line appears once, the hold (8 in-flight lines, ~7.5 s) is entirely before any `createGroup`; the flaky Carlinkit link never completed an AAP session (`SSL`=0, same as round 1), so the 8 s budget genuinely fired — and now it *says so* instead of the round-1 silence. `USB handoff settled after` did not occur and should not have here. **R6 FAIL.** Overlap *detection* works — `NativeAA: another poke is already connecting to POCO X3 NFC — waiting for it rather than opening a second socket.` fires **exactly once, 3/3 runs** — but `PokeOverlapPolicy.CONNECT_SETTLE_WAIT_MS = 4000` is sized for the reporter's ~3 s active refusal, and with the POCO's Bluetooth genuinely `state: OFF` this rig's `socket.connect()` runs the full **15.40–15.46 s** RFCOMM timeout. So the manual poke's 4 s wait expires, `PokeOverlapPolicy.step()` returns `PROCEED`, and it opens its own socket ~11 s before the automatic one closes — two concurrent RFCOMM sockets to one phone, every record failing, no session (the same `read ret: -1`-on-every-record symptom the commit set out to remove, 3 failed records instead of 4). The brief's §4 assumption ("its `socket.connect()` then blocks for the ~3.05 s a refusal takes") is wrong for a phone that is *off* rather than *present-and-refusing*. `e68636db` is fully hardware-clear; `5ef1012d` needs a fix. **Rig notes:** R6's timed trigger must be a file poll, not `logcat \| grep -m1 … && send` (that pipeline hangs after the match — `adb logcat` never exits — so the broadcast never lands; first R6 attempt was void for this). The POCO would not rejoin WiFi through any adb verb (`Uid 2000 does not have access to enable-network`/`reconnect`, `svc wifi` cycle inert) — needed an operator tap onto `Pegue Cdesta` and a physical PC-cable→dongle swap on its USB port for R5. MT50 `settings.xml` restored (identical to backup); POCO keeps the debug candidate, `wifi-direct-band` restored to `0`. New scripts `wireless_bringup_5ghz_r2_poke.sh`, `wireless_bringup_5ghz_r2_usbhold.sh`. | **Nothing queued.** `e6ed1ad2` (`PokeOverlapPolicy.CONNECT_SETTLE_WAIT_MS` 4000→20000 + `Step.ABANDON` + the `awaitPokeSlot` wait on `triggerPoke()`'s device loop) is hardware-clear across R0/R1/R6/R7/R8. R6's overlap is serialised on every run; the 20 s abandon shape and the automatic-loop wait are JVM-only on this rig and stay unmeasured on hardware (R9 = 0 sightings, as expected). R2/R3/R4 stay settled from round 1, R5 from round 2. |
| `usb-aoa-dongle` | **Round 2 done — R0/R10/R11/R2 PASS, R7/R8 UNTESTABLE, R3 INCONCLUSIVE, on `testing/wireless-plus-automation` `1f4b85ce` (stamp `1f4b85ce5a59`, gate 1414/0, md5 `b892fcf6cc3740bee42e254ba8277559`). Results `usb-aoa-handoff-round2-results.md`, evidence `evidence/usb-aoa-handoff-round2/`.** **R7/R8 (`ee16f522`, the point of the round) UNTESTABLE:** a phone in MTP mode (`FF/FF/00` named MTP, no default handler) on a phone host is short-circuited by AOSP's `resolveActivity` (§7b) — the `USB_DEVICE_ATTACHED` intent never reaches `UsbAttachedActivity`, there is no manifest receiver for it, so `ee16f522` (drop the `0x18D1` gate) cannot be exercised by plugging a phone here; `r7-ohu.txt` is empty, confirmed with a full-logcat replug. Coverage stays on `UsbAttachPolicyTest`. A `FEATURE_AUTOMOTIVE` USB host would be needed; the MT50 can't host USB. **R10 PASS** (regression guard): `Switching USB device to accessory mode` → first `Success controlTransfer len: 2 acc_ver: 2` = **14 ms**, 0 `Error controlTransfer len: -1`, full SSL session + video (14 ms < `main`'s 100 ms so `USB_TIMEOUT_IN_MS=1000` isn't *exercised* — the slow-device demo was routed to R7). **R11 PASS**: `UsbAttachedActivity` absent from recents + activities, 0 `Usb re-attached in normal mode`, every attach → fresh `onCreate` (2nd on a new process), `onNewIntent` 0 — the manifest `noHistory`/`excludeFromRecents`/`taskAffinity=""` make the lingering path unreachable. **R2 PASS**: libusb route, one native `Performing AOA switch` (~22 ms), 0 `didn't handle`, `Successfully connected via JNI Libusb`, **0 `Unable to find an endpoint pair on the accessory interface`** (the shared `UsbEndpointSelectionPolicy` picking libusb endpoints works), SSL + ~55 s video `dropped=0 skipped=0 concealed=0` HEVC HW — clean on the exact link R3's standard route only made 3/5. **R3 INCONCLUSIVE** (standard link degraded, pre-registered §3; `num_connects` ran 211→275, 2/5 SSL failures = `#800` signature): `Sending acc start` = **5** (one switch per cycle, no double-switch), but `didn't handle` = **2/5** (cycles 2, 5) — both are `ee16f522`'s activity→service hand-off after a slow accessory-permission re-request, **NOT** the narrowed `UsbSwitchClaim` "leaving to it" path (fired 0 times — no second attach ever raced; the dongle transitions as one device). The regression-guard question (does the narrowed claim still cover a *racing* attach?) was not exercised; what surfaced is `didn't handle` on permission re-request latency, for the code side to assess. Round 1's 0/10 was a dongle that kept permission / re-enumerated faster. **No `systemui.usb.*` dialog in any capture** — the dongle's `2D00` auto-grants silently, so the "third dialog identity" watch item does not arise. First R3 capture died (wireless adb dropped during fast replugging) → re-run as `r3b` at a slower cadence; an inline `adb logcat &` from one Bash call doesn't survive it, `capture.sh`'s split start/stop does. Settings restored (residual: two app-written `last-loc-*` keys). New script `round-usb-aoa-handoff-r2/capture.sh`. ~~round 2 queued~~ Round 1 done (`usb-aoa-handoff-round1-results.md`) — nothing blocked the branch. Rig: Carlinkit-class AA dongle (idle `18d1:4ee1`, accessory `18d1:2d00`) in the **Poco X3 NFC**'s USB-C port (OTG), Poco as head unit running HUR `githubDebug`, **Motorola edge 30 neo** as the projecting phone over the dongle's own radio/BT; wireless adb (`adb tcpip 5555`) because the port is occupied. **Session itself is clean** end to end: AAP 1.7, SSL ~60ms, HW H.264 1080p60, ~50fps, 0 dropped/skipped/concealed; nothing to change in transport/SSL/decoder. **The defect is the AOA switch→open handoff**: the dongle holds accessory mode ~390ms when nothing claims it, and the real failure is permission — the auto-grant for a re-enumerated `2d00` arrives with the manifest-matched activity launch, not with the attach broadcast the service sees ~40ms earlier, so we raised a dialog the dongle never waited for. **libusb route** (`use-libusb=true`) also connects clean; one run each side so far, not shipped default. **#800 re-read and the earlier entry here was wrong twice**: the ~90s zombie window is *not* a HUR gap (the phone was still sending, `video=0kB/s (15 msgs)` / `rendered=5,fed=5` per 5s ≈ 1 fps, which is AA's idle rate) so the transport-restart proposal is withdrawn; and the handoff findings do not help that reporter (their reconnect was 740ms, one switch, no prompt). What the log does add: our own `LinkGapMonitor` scored the link **24% dead** (`dead=7267ms, longest=3029ms` in 30s) during the *good* window, and the unit runs its own OEM Bluetooth module (`Goc ... GBTSD4`) cycling `unregister_AA_uuid` → `register_AA_uuid` → `send_spp_connect` dead-regular at **17.5s**, 29 times. The two #800 reporters are different faults: USB 1.1 fixed JHLEU outright and did not fix s4toruu-x. Findings: `usb-aoa-dongle-findings.md`. | **Round 1 done 2026-09-03** on Poco X3 NFC host (`192.168.1.10:5555` wireless adb), candidate `fork/fix/usb-attach-clear-defaults` @ `361cfed3` / baseline parent `e3077047`. APK md5 `ec0fcc99…` / `819bb763…`. Results `usb-aoa-handoff-round1-results.md`, evidence `evidence/usb-aoa-handoff-round1/`. **R0 PASS** (identity 7/0, unit **1010/0** & **997/0**, exactly the two commits). **R1 INCONCLUSIVE** — handoff clean 3/3 (exactly one switch, 0 `didn't handle`, `holding the wireless bring-up` fired, `quiesceWirelessForWiredSession` ran, dongle patience 408–411 ms) but **no session forms**: SSL handshake fails every attempt (`Unable to parse TLS packet header`) on a corrupted AOA bulk stream (`USB reads recovered after 2 errors`). The dongle-findings run got clean 60 ms SSL on this rig days earlier — the standard-route USB link has since degraded (progressive marginal-USB-2.0, `#800` signature), so the fix-vs-baseline race the round wanted is unanswerable here. Baseline arm not run (would fail identically). **R2 PASS** — the libusb route (`use-libusb=true`) forms a full clean session on the **exact** link R1 fails: `SSL handshake complete`, ~50 fps, 0 dropped/skipped/concealed over 2.5 min, one native switch, 0 `didn't handle`. **R3 (a std / b libusb) — switch-claim PASS, sessions INCONCLUSIVE**: 0 `didn't handle` on every one of 10 replug windows across both routes (the `didn't handle` events in the captures are all the post-session-failure ~20 s service-retry loop, not fresh-plug races); libusb connected 5/5, standard 4/5 enumerated cycles formed sessions. **R4 PASS** (libusb, deviation) — dongle already at `2D00` when the service started: `holding the wireless bring-up` fired at +73 ms and held everything (no 5288 bind / no `ACTIVELY LISTENING` / no poke) until `quiesceWirelessForWiredSession` at +729 ms. **R5 PASS** — no USB device: deferral line absent, wireless armed at **75 ms (cand) vs 76 ms (base)** — free. **R6 PASS** — `TaiYiLian UGREEN-BT701` (`0A12:4007`) audio adapter `rejected: no Android interface` / **0 usable** on both builds (not `accepted`, so no §7b pre-existing fault), no AOA switch, deferral absent, wireless 85 vs 75 ms. Notes: `createGroup SUCCESS` never occurs on this Poco (mode 3 over SoftAP, `native-ap-transport=1`) — R5/R6 use `onCreate`→`WirelessServer: binding port 5288` as the proxy, valid since the deferral is upstream of the transport branch. `wifi-connection-mode` was observed to change 3→1 mid-round (cause not isolated — reinstall or USB-attach path; restored). The manual "USB Connect" button succeeds on the standard route where the auto cold-plug fails — points at opening the AAP handshake before the dongle bridge settles. New script `round-usb-aoa-handoff-r1/capture.sh`. Candidate left on the Poco, `settings.xml` restored. **Round 2 queued: `usb-aoa-handoff-round2-brief.md`.** Round 1's SHAs are all gone — the branch was re-ported onto a refactored `main` on 2026-09-03 and reviewed and rewritten on 2026-09-05, and is now `ee16f522` + `b236103c` on `main` @ `c4dd2ba1`, built as `testing/wireless-plus-automation` @ `1f4b85ce` (stamp `1f4b85ce5a59`, gate **1414 / 0**). **No baseline APK** — round 1's baseline was the branch's own parent and no longer exists, so the one run needing a control gets it from a settings change on the candidate. **The point of round 2 is `ee16f522`, the non-Pixel attach, which round 1 had on both arms and explicitly did not test**: the 0x18D1 gate is gone from both the attach activity and the service fallback, and `UsbAttachPolicy` now reads an absent `allow-devices` as never-configured rather than nothing-permitted. R7 plugs a non-Pixel phone (Motorola, **USB debugging off** per §7b) with no allow list and wants `Switching USB device to accessory mode`; R8 is the control, an allow list holding one bogus entry, wanting `Not switching ... letting the service decide` **then** `Fallback: force=true and found single normal-mode Android device` from the service. Three things changed under round 1: `USB_TIMEOUT_IN_MS` is 1000 (round 1 tested 500, `main` is 100) — R10, judged on the timing not the verdict; `UsbSwitchClaim` is no longer ORed into `isSwitchingToProjection()` and is read only by the attach fallback — **R3 is the regression guard for that**; and the activity's 3 s accessory poll and background `startForegroundService` were removed, so round 1's `Accessory mode reached after` / `Device never re-enumerated` lines no longer exist. R11 is new for the manifest `noHistory`/`excludeFromRecents`/`taskAffinity`. R2 re-runs because `UsbEndpointSelectionPolicy` now decides the libusb route's endpoints too. **Round 1's R4 and R5 are retired here** — `WirelessBringUpDeferralPolicy` moved onto `fix/wireless-bring-up-and-5ghz-channel` and its round 2 already passed it. R7/R8/R10/R11 need no dongle and run first, so the degraded standard-route link cannot cost the whole round; R3/R2 are pre-registered INCONCLUSIVE on the same terms that made round 1's R1 inconclusive, and that item still stands — it needs a better cable, a fresh OTG adapter or a second dongle. **Round 2 outcome:** R7/R8 turned out UNTESTABLE on the phone host (MTP short-circuit — `ee16f522` needs a `FEATURE_AUTOMOTIVE` USB host or the JVM test); R10/R11/R2 PASS; R3 INCONCLUSIVE with a finding (`didn't handle` 2/5 on accessory-permission re-request latency, not a `UsbSwitchClaim` failure). Still open for a real hardware verdict: `ee16f522`'s non-Pixel attach (needs an automotive USB host) and a clean R3 (needs a non-degraded dongle link — fresh cable / OTG adapter / second dongle, unchanged from round 1). Nothing blocks the branch; the `didn't handle`-on-permission-latency behaviour is worth a code-side look. |
| `driver-selection-native` | **Round 6 done — all six runs PASS on candidate `7520686c` (stamp `7520686c2578`), unit gate 1357/0, md5 `55651f18912fda91fbae92c2b6c3e713`. The two new commits (`876627e3` switch-wake guard, `c77806ac` selector labels) are hardware-clear; nothing queued.** **R14 PASS** (all six criteria): with the outgoing phone D-MOTO holding a live hands-free link (`HeadsetClientStateMachine curState=Connected`), the switch to D-POCO logged `NativeAA: Calling socket.connect() for POCO X3 NFC` at pick+27 ms and `... even though a hands-free link is up — that link is another phone's, not this one's.`, RFCOMM landed at socket.connect+1.389 s, second `SSL handshake complete` on `192.168.49.50` (vs D-MOTO's `.189`), one `createGroup SUCCESS`, zero `Not poking POCO`, `... is not let straight back in.` present. Round 5's two failed attempts had zero `Calling socket.connect()` for D-POCO — the fix is confirmed. **The R14 precondition needed forcing:** after a Native AA session forms the poke socket closes at handoff and the projecting phone drops its BT profile link; two toggles of D-MOTO's *own* Bluetooth did nothing, toggling the *head unit's* Bluetooth adapter reconnected both phones and gave a stable `Connected` HFP link. **R15 PASS**: with D-POCO Bluetooth off, the Switch-Phone selector row read `Disconnected, will be woken` (D-MOTO row `In vehicle / Connected`, not the unreadable-stack `Paired`), and tapping it logged `btConnected=false` + `mode=PILL_THEN_OVERLAY` with the pill reading `POCO X3 NFC is disconnected, waking it...` (`auto_connect_pill` in the dump, no overlay). Note: `ACTION_NATIVE_AA_POKE` shortcuts `selectDriver` and never drives the pill path — R15's pick has to be a real `input tap` on the selector row; also a BT-connected preferred phone makes launch skip the selector entirely (`Unambiguous driver ... without prompt`), so the selector comes from Switch Phone on a live session. **R12 PASS** (`btConnected=true`, `mode=OVERLAY`, `auto_connect_loading_overlay` present / pill absent). **R10 PASS** (both radios off: `btConnected=false`, `mode=PILL_THEN_OVERLAY`, pill present / overlay absent, pill text `motorola edge 30 neo is disconnected, waking it...`, poke still fires, PROMPT_SHOWN→pick 9.999 s, watchdog hides pill at +30.0 s). **R1 PASS** (5 pokes alternating both phones, first at createGroup+2.220 s, zero `not phones`, zero `Multi-driver selection is active`). **Watch item clean:** the override line fired zero times in R1 and R10, where no hands-free link exists. RFCOMM reconnect flood was 602 cycles in R14 (round 5: 282) — bounded, log-volume only. Results: `driver-selection-native-round6-results.md`, evidence `evidence/driver-selection-native-round6/`. `settings.xml` restored byte-identical, candidate left on D-HU. **Post-round, not measured:** `8bed7833` renames the reachable-case overlay text from `Native-AA driver: %s` to `Starting Android Auto with %s` (a user read the old string as a debug label); resource string only, judged-by-view-id runs unaffected, `testing/...` re-merged to `1c87d607`. **Branch compacted 2026-09-05:** the six commits of our own are now two above `origin/driver-change-native-mode` (`d103ce7a`), split by component: `02b02a76` the link side and `3ed0bac2` the UI side, tree byte-identical to `8bed7833` plus the log change below. Old SHAs are held by tags `driver-selection-pre-compaction` (`8bed7833`) and `driver-selection-testing-pre-compaction` (`1c87d607`), so `7520686c`, the candidate this round measured, stays reachable. One unmeasured change rides on the feature commit: the `Connection accepted from` line is skipped for a phone the gate is already turning away, and the refusal is restated with its running count once a minute, which takes an R14-shaped run from 602 lines to about five at every log level. New testing tip `cce12836` (stamp `cce12836ca6a`), unit gate 1361/0. | **Nothing queued.** The branch (`02b02a76` + `3ed0bac2` on `fix/native-driver-selection-headless`, base `origin/driver-change-native-mode` at `d103ce7a`) is hardware-clear across all six runs and is PR-ready. Round 5's withdrawn "disconnect the outgoing phone's audio profiles" recommendation stays withdrawn — round 6 confirms the switch-wake guard (`linkIsAnotherPhones`) is the right fix and needs no profile teardown. |
| `narrow-band-and-disconnect-scope` | **round 1 done — R1 PASS (the point of the round), R2/R3/R5 PASS, R4 INCONCLUSIVE (pre-registered). All four commits clear on hardware; nothing queued.** | Run 2026-09-03 on candidate `9635f8a5` (md5 `91d08500…`, 1259/0) + control `a938ba91` (md5 `92ec9685…`), D-HU = MT50, phone = POCO X3 NFC / GH 17.5. **R1 PASS**: `AapTransport stopping and sending byebye` **after** the step-2 `headunit://exit` service destroy — candidate **1** (21:31:26.355, after the step-4 `headunit://disconnect`), control **0** (step-4 intent received, `onDisconnected` ran, no byebye). Both builds send exactly one byebye at the step-2 synchronous `destroy()`, and one `AapService destroying...` total. Exactly the brief's "Expected 1 and 0". **R2 PASS**: `showing the connection issue banner for VIDEO_LINK_TOO_SLOW` on both `native-ap-transport` 0 and 1 (force-stop+relaunch each), banner text on screen, seed stamp intact after both. **R3 PASS**: after ~56 s of rendered video (`First frame rendered` 21:35:51, ~50 fps, dropped=0), `headunit://disconnect` → `connection-issue-video-starved` reads `0`; relaunch shows no banner. **R5 PASS (regression-guard branch)**: rig answered `WifiDirectManager: this unit's WiFi radio reports a 5 GHz band` (needed a forced fresh `createQuietGroup` — a warm group is refreshed, not recreated, so the band line doesn't emit); `[RES_CAP] … linkCapped=none`, no `no 5 GHz band` line, profile announced unchanged at 1920x1080@60. **R4 INCONCLUSIVE (pre-registered)**: SoftAp config unreadable/unwritable on every attempt (`SecurityException: App not allowed to read or update stored WiFi Ap config`), so the literal PASS string `HotspotManager: WiFi disabled before enabling hotspot.` is unreachable — API 34 ignores an app WiFi-disable, code logs `Asked to disable WiFi and the platform ignored it` after the full 8.04 s settle wait. What matters held: an AP **did** come up (`wlan2` appeared after attempt 2's enable request; attempt 3 formed a full session + video over the manual-override SSID), and **no** give-up line blamed privileges (`canWriteSettings=true`, non-privilege arm both times). Not FAIL. **Brief errata**: (a) the pref key is `log-level` (enum ordinal, VERBOSE=0), and the rig was on 2/INFO not VERBOSE — §4's "as the rig already sets" is wrong, set it to 0; (b) §5's `WiFi disabled before enabling hotspot.` and `on this API level: <Exception>` strings don't occur on this rig (see R4); (c) MT50 station recovered from OFF via plain `svc wifi enable` + autojoin twice this session, no operator tap — contra §7a. Results: `narrow-band-and-disconnect-scope-round1-results.md`, evidence `evidence/narrow-band-and-disconnect-scope-round1/`. Candidate left on D-HU, `settings.xml` restored byte-identical. **Nothing queued — the branch's four commits are hardware-clear.** A fifth, `68fbc392`, shortened the R2 banner's text after the round (the screenshot showed it wrapping over most of the panel); resource string only, nothing measured changes. |
| `aa-numeric-keyboard` | **round 2b done — R1 PASS. AA 17.5 renders `TelephoneKeypadTemplate` for a non-`CALLING` POI app. Nothing queued; the open lever is answered.** | Run 2026-09-02 on D-HU (MT50, OHU `3.3.0` vc103, md5 `0abee20c…`, mode 3) + D-POCO (POCO X3 NFC, AA `17.5.663204`). Probe `aa-keyboard-probe-r2b.apk` md5 `e40ec5ac2a9f9c156846f74a44dbbafe`. Pre-flight: all five templates `OK`, no crash — round 2's `onGetTemplate()` builder-constraint crashes are fixed and the r2b "Back to list" body action works. **R1 PASS**: `16:58:08 Host received new template (type: androidx.car.app.dialer.TelephoneKeypadTemplate)`, no `Error`, no calling-category refusal; `Raw list of template calling apps found: []` every enumeration (probe is a `template` app, not `CALLING`) and the keypad still drew. Digits reach the field (`12345`); `*` and `#` accepted as literal chars (`12345*#`); check-mark action fires with a `GhToast` `entered: 12345*#`. **No decimal key** — the `*` (labelled `dec?`) emits `*`, not `.`, so a fuel-amount `12.5` needs the app to parse `*`/`#` as a separator. **R2 PASS**: rows A (`KEYBOARD_NUMBER`) and D (`KEYBOARD_EMAIL`) render the identical full QWERTY, matching round 1 — build sound, R1 trustworthy. **R3 FAIL (predicted, not verdict-affecting)**: `enable-rotary=true` announces `Rotary/Touchpad support` but still `hasTouchScreen=true` / `touchscreenType: 1`, so Gearhead keeps the touch keyboard; row C rendered full QWERTY, no dialpad. Results `aa-numeric-keyboard-round2b-results.md`, evidence `evidence/aa-numeric-keyboard-round2b/`. Round 1 (`aa-numeric-keyboard-round1-results.md`) settled `setKeyboardType()` is inert for all four constants — `TelephoneKeypadTemplate` sidesteps it by drawing its own keypad. **App-developer answer:** for a POI/template app on AA 17.5, `TelephoneKeypadTemplate` is a usable numeric-keypad lever; the only gap is the missing decimal separator. |
| `pr-readiness` | **round 1 done — §4 PASS 3/3, §5 PASS, §6 (6.1–6.7) all PASS incl. the 6.4 credential-withhold. All three pull requests hardware-clear.** | Run 2026-09-02 on **one merged APK** `fork/testing/automation-plus-btautostart` @ `e9ab7f13` (= `pr/bt-auto-start` `99eb41a7` + `pr/automation-command-surface` `773e8e77`, both ancestors; one trivial merge conflict in `AapService.observeConnectionState()` resolved keeping all three arms). md5 `0abee20c…`, `1247/0` (superset of A 1223 / B 1010), all R0 DEX symbols present. **§4 PASS 3/3**: the station stand-down produces exactly **one** `startNativeAaQuietHost()` chain and **one** `5GHz createGroup SUCCESS!` per iteration (`p2p-wlan0` `-0`/`-1`/`-2`), claim line + `StationStandDown … has left` both present, session forms each time, `reported N off/on state changes` warning **absent** — the last round's R6 double-chain is fixed (claim staked before the `WIFI_P2P_STATE_CHANGED state=2` broadcast, so the receiver defers). Noted: `disableNetwork()` returns false on this rig but `disconnect()` still stands the station down; the restore logs a refusal but autojoin re-associates between iterations (no manual tap this round). **§5 PASS** (after the operator enabled D-POCO's AA-dev "Start head unit server" `:5277`): combined APK installed on D-POCO, Self Mode configured (`connection-modes={self}`, `wifi-connection-mode=1`, `auto-start`/`auto-disconnect-bt-macs={11:46:03:10:33:59}` = D-HU's real adapter MAC). A live Self Mode session aged ~79 s, then D-HU `svc bluetooth disable` → `went away; ending the session in 5000ms` → `stayed away; ending the session the way the Exit button does.` → `session state disconnected (user_exit)` → `Self Mode disconnected. Not restarting.` → adapter self-revert → `BT Device connected: Navegadortz2 (11:46:03:10:33:59)` → `MATCH! Starting AapService` → `forcing a Self Mode launch` → `SSL handshake complete` (fresh session). 60 s age guard did not fire (session old enough), `kill-on-disconnect=false` so pid not killed. Note: plain `MainActivity` launch does not auto-start Self Mode on this config — the first session came from the auto-start trigger itself. **§6 PASS**: 6.1 commit reported (`e9ab7f135ec7`, contains `773e8e77`); 6.2 commands accepted, session start/end, state replies differ; 6.3 GET_SETTINGS + SET_LOG_LEVEL refused with switch off, `log-level` unchanged; **6.4 zero credential leak** — 0 hits for every bait value, `withheld:6`, none of the 6 withheld keys in the reply; 6.5 private path + `/sdcard` alias both refused (fail-closed) and `settings.xml` untouched, `/storage/emulated/0/Download/…` succeeds; 6.6 `connecting→connected→projecting→disconnected(user_exit)`, SessionStateIntent structurally carries only 4 non-credential extras (no rig receiver to dump extras empirically); 6.7 marker at WARN. **Brief errata found:** (a) `raise projection`/`refresh sensors`/`restart audio` are registered as `com.andrerinas.openheadunit.aap.action.RAISE_PROJECTION` (etc.), **not** `…ACTION_RAISE_PROJECTION` — the brief's 6.2 shorthand gets `unknown action`; (b) 6.5's `/sdcard/Download/` must be `/storage/emulated/0/Download/`; (c) `ACTION_LOG_MARKER` is gated by `allow-external-configuration` (in `CONFIGURING`), so it can't separate the un-gated 6.1/6.2 runs as the brief suggests. Results: `pr-readiness-round1-results.md`, `evidence/pr-readiness-round1/`. New scripts `pr_readiness_s4.sh`, `pr_readiness_s5.sh`. **Nothing queued — all three PRs can open.** <br><br>**Comment sweep after the round, 2026-09-02:** `pr/bt-auto-start` is now `255d74d1` and `pr/automation-command-surface` `4c09cbc2`. Comment text only - `git diff` against the tested `99eb41a7` / `773e8e77` touches nothing but comment lines, verified, and both test counts are unchanged (1223/0 and 1010/0). **The round's result stands; no re-run needed.** `pr/native-aa-wireless` is untouched at `133e196d`. The testing branch was rebuilt on the swept tips: `testing/automation-plus-btautostart` is now **`823a184e`** (merge of `255d74d1` and `4c09cbc2`, same `AapService.observeConnectionState` resolution keeping `sessionConnectedAt` plus the `Connecting` arm and both `emitSessionState` calls). `git diff e9ab7f13 823a184e` touches comment lines only, and the gate is still **1247 / 0**. Rebuild from `75f9ae83` if another APK is needed. |
| `field-ride-automation-btautostart` | **rounds 2 + 3 done — R1-R5 all PASS on both device pairings; the branch is PR-ready.** Round 2 = MT50 headunit / POCO phone. Round 3 = POCO headunit / Moto phone (roles swapped): R4 needed a re-run (R4b) — see notes. @ `1f06d8a83224`, md5 `3f53e72f…`, 1266/0 unit tests. | **Serials/roles** (also in `TESTING-TEMPLATE.md` §7a going forward): `27870808938846` = MT50, fixed motorcycle headunit (root). `4f4027e9` = POCO X3 NFC `DC:B7:2E:5E:4E:59`, sometimes phone / headunit / Self Mode, **not rooted**. `ZY22GC3BM4` = Moto edge 30 neo `A0:46:5A:97:E4:95`, always phone (also the OTG USB host). <br><br>**R1** (Native-mode Bluetooth auto-start): after the trigger `forcing a Self Mode launch` = 0, `createGroup SUCCESS!` = 0, `leaving Self Mode alone` = 1; session then formed `connecting→connected→projecting` clean. The brief's HU-side `svc bluetooth disable` method was **INCONCLUSIVE** (phone reconnects and forms a session in ~20 s, so `AutoStartReceiver`'s pre-existing `[FIX] already connected` guard — an `AppLog.d`, invisible at INFO — ate the post-trigger ACL, no `MATCH!`); re-ran with phone radios off at launch + phone-BT-on as the trigger, which fired the receiver with `isConnected=false`. **Contra §7a, cycling the phone's BT on DID raise `ACL_CONNECTED`** on both pairs. **R2** (Helper-mode control): same trigger → `forcing a Self Mode launch` = 1, `leaving Self Mode alone` = 0 — veto is Native-only (`BtAutoStartActions(… forceRearmWireless=false …)`). Round 3's R2 is the stronger F2-fix demo: the forced Self Mode launch genuinely **fails** (`All launchers failed`, AA 17.4+ / no `:5277`) yet emits **no false `user_exit` and no auto-poke suppression**. (Round 2's R2 launch happened to succeed via a loopback 5288 session on the POCO-as-phone — also fine, `All launchers failed` = 0 there.) **R3**: all three audio channels announced on a real Native session — `AUDIO2` / `AUDIO1` / `AUDIO`, each `status: HEADUNIT`; skip-reason lines absent (fix exercised). **R4**: a completed BT handshake writes `native-poke-bt-macs={phone MAC}` (`NativeAA: Saving … as the wake poke device.`) while `auto-start-bt-macs` stays `<set … />` empty — the write mechanism works (positive control), so the empty auto-start list is a real negative. On the POCO↔Moto pair R4 was INCONCLUSIVE first (the Moto rejoins the POCO's cached Wi-Fi Direct group directly, skipping the handshake; nothing in AA's `Vehicles` list to forget, and `cmd wifip2p` is root-only) → re-run as **R4b** after setting `wifi-direct-stable-identity=false` + deleting the POCO's saved `wifi-direct-group-*` keys so it presents a fresh group: the handshake then ran and R4b PASSed. **R5** (guard only): 4 in-process bring-ups, `listening … TCP 5299` ×4, `could not listen` / `EADDRINUSE` = 0. <br><br>**Gotchas:** `SelfLauncherManager` reads the AA installed on the **headunit device** (MT50 bundled `gearhead:car` = 17.3 in round 2; POCO Gearhead = 17.5 in round 3) — round 2 results corrected for a mis-attribution to the POCO phone. `ACTION_LOG_MARKER --es text` truncates at the first space over `adb shell` (wrap the whole `am` in one quoted string). `ACTION_NATIVE_AA_POKE` is inert outside Native mode. POCO↔Moto ACL reconnect is flakier than MT50↔POCO (only PAN policy=100 between them) — R3-round R2 trigger had to be injected via `am start --es launch_source 'Bluetooth auto-start'`. **New script:** `set_hu_settings_runas.py` (run-as `<set>`-aware pref editor for the non-rooted POCO). Settings restored byte-identical on both devices. <br><br>Results: `field-ride-automation-btautostart-round2-results.md`, `-round3-results.md`; evidence `evidence/field-ride-automation-btautostart-round2/`, `-round3/`. Round 1: `-round1-results.md` / `-round1-brief.md`; briefs `-round2-brief.md`. **Nothing queued — the branch can open a PR.** |
| `automation-followups` | **queued — a short round, three checks** | `pr/automation-command-surface` @ `c8aef187` (a third commit on the two tested ones); testing branch `75f9ae83`, gate **1252 / 0**. Answers the three findings round 1 raised. **A1:** `ACTION_LOG_MARKER` with *Allow external configuration* **off** must now write `AutomationMarker: <text>` at WARN instead of being refused, and every other configuring verb must still be refused with the switch off. **A2:** each of `ACTION_RAISE_PROJECTION`, `ACTION_REFRESH_SENSORS` and `ACTION_RESTART_AUDIO` must be accepted under both spellings — the documented `com.andrerinas.openheadunit.ACTION_X` and the original `com.andrerinas.openheadunit.aap.action.X` — sent implicitly (`-a` with no `-n`) so the manifest filter is exercised, not just the explicit path. **A3:** with the switch on, `ACTION_GET_SETTINGS --es path /sdcard/Download/x.json` must now succeed and write the file, while `/data/data/<pkg>/shared_prefs/settings.xml` and `/sdcard/Android/data/x` must still be refused. Delete the written file afterwards and turn the switch back off. |
| `wifi-direct-stable-identity` | **round 4 done — R1+R2 PASS, fix stack PR-ready; R6 (stand-down) is a benign FAIL** | Run 2026-09-02 on **`f0c20b8a`** (the bt-auto-start candidate, which is `0a9a98d9` + 2 commits that don't touch WifiDirectManager/handshake), baseline `937e68b7`. APK md5 cand `190488f9…` / base `3e5cb007…`, both R0 gates PASS (1208/0 and 1184/0 — cand count is the post-rebase 1208, not the brief's 1193). **R1 PASS**: baseline faulted 3/3 (double `startNativeAaQuietHost` ~170-220 ms apart, `refresh: no group is up`, 2× BUSY, 3 groups, phone `NETWORK_NOT_FOUND`×2); candidate clean 3/3 (one chain, refresh = WAIT every time, session formed). **R2 PASS**: candidate Type 3 matched the live group on all 6 sends, `the live credentials still match` present, phone `NETWORK_NOT_FOUND`=0; baseline differed exactly where R1 faulted. **R3/R4/R5 PASS** (settings-pause defers the poke and never stops the loop; auto-reconnect via HU-bt cycle forms a session with one createGroup; 5-min control clean, `dropped=0`). **R6 FAIL (benign)**: the stand-down claim line appears, but **two** `startNativeAaQuietHost` chains at bring-up — `WIFI_P2P_STATE_CHANGED → P2P enabled, auto-starting` races the `StationStandDown` completion callback. Both claim the window, both createGroups succeed first-try, sessions form with no BUSY/`NETWORK_NOT_FOUND`, so it doesn't break on this rig — but `0a9a98d9` does not close the second chain the brief wanted gone. Results + evidence: `wifi-direct-and-bt-auto-start-round1-results.md`, `evidence/wifi-direct-and-bt-auto-start-round1/`. **The WiFi-Direct fix stack is PR-ready on R1+R2; R6 needs a look before relying on `stand-down-station-for-wifi-direct`.** Brief §3/§4's "log-level=2 for the DEBUG lines" is inverted — DEBUG lines need ordinal 1 (enum is VERBOSE 0, DEBUG 1, INFO 2); ran everything at 1, and the brief is corrected in place. <br><br>**R6 answered in `7cd74b5d`** (branch tip now `7cd74b5d`, 9 commits, 1195/0). The receiver's opportunistic auto-start did not read the create window the stand-down claims 1.5 s ahead of its own bring-up, so `P2pStateChangePolicy.shouldStartBringUp` gained a `createClaimed` arm and `WifiDirectManager` passes `isCreatingGroup`. **Next round: re-run brief 1 R6 only** against `7cd74b5d` (or the child tip) with `stand-down-station-for-wifi-direct=true` — PASS is one `startNativeAaQuietHost() requested` after the claim line instead of two, and one `createGroup SUCCESS` at bring-up instead of two. R1-R5 do not need repeating; nothing they score changed. **Superseded: the R6 re-run is now §4 of `pr-readiness-round1-brief.md`.** The branches were regrouped into three pull requests, so round 5's candidate SHA no longer exists; its brief was removed rather than left pointing at an orphan. |
| `bt-auto-start-disconnect` | **round 1 done — auto-disconnect proven end-to-end in Self Mode (R6/R7 PASS); Native-AA lever INCONCLUSIVE by a rig limit that is itself the finding; branch PR-ready pending a settings-hint wording** | Run 2026-09-02 on `f0c20b8a`, md5 `190488f91a34fcbeaf2d653496e7bd4c`. **R0 PASS** (1208/0, `BtAutoDisconnectPolicyTest` 10, `BtAutoStartRearmPolicyTest` 18, both DEX symbols present). **R6 PASS (D-POCO Self Mode, the real end-to-end test)**: MainActivity launch → no session; D-HU adapter off → `went away, but nothing is projecting` → self-revert → `BT Device connected … MATCH! … BtAutoStartActions(clearUserExit=true, forceRearmWireless=false, armWirelessIfIdle=false) … forcing a Self Mode launch … SelfMode: AA 17.4+ detected … SSL handshake complete` (session 1, by the trigger); then adapter off during the settled session → `went away; ending the session in 5000ms` → `stayed away; ending the session the way the Exit button does` → `Hiding reconnecting overlay` → `User exit cooldown` → `Self Mode disconnected. Not restarting.` → `keeping the wireless bring-up down` → (2.9 s) → `MATCH! … forcing a Self Mode launch` (session 2). 2 starts + 1 end by the trigger, no relaunch between, both `MATCH`es corroborated by phone `ACL_CONNECTED`. **R7 PASS**: same with `kill-on-disconnect=true` — after `stayed away` the OHU pid went 10534→11106 (killed + cold-started by the manifest receiver on the revert), `App launched via: Bluetooth auto-start`, fresh session. **R8 PASS** (Helper): auto-start fires (`forceRearmWireless=false`) and the P2P group is **not** torn down (`Initializing WiFi Mode` stays 1). **R9 PASS**: empty list = 0 `Bluetooth auto-disconnect:` lines, session survives (phone link was up this run, so it's genuinely the off switch). **R1/R1b/R1retry/R2/R3 INCONCLUSIVE**: on this HU the projecting phone drops its BT ACL ~0-5 s after every WiFi handoff and doesn't restore it during the session (**R4 = PASS, outcome B**: `not ending the session … up=true, age=Nms` with N∈{5724,9425,9479,30720}, the 60 s guard holds every time), so the deliberate "watched device leaves after 60 s" lever has no ACL to drop. **R5 PASS** (poke's ACL cycling → `went away, but nothing is projecting; leaving the connection stack alone`, nothing torn down). Results: `wifi-direct-and-bt-auto-start-round1-results.md`, `evidence/wifi-direct-and-bt-auto-start-round1/`. **Settings-hint wording to add:** the watched device must keep a BT profile with the head unit through the session — the phone being projected does not qualify on this hardware. Needs `:5277` (operator toggled it mid-round); D-POCO `wifi-connection-mode` set to 1 for R6/R7 (restored to 3). Candidate left on D-HU **and** D-POCO, both `settings.xml` restored byte-identical.<br><br>**Rebased onto the R6 fix**: tip is now `16965e75` (2 commits on `7cd74b5d`, 1210/0); `f0c20b8a` is kept reachable as tag `bt-auto-start-round1-tested-20260902`. Nothing this round scored changed, so brief 2 does not need repeating — a rebuild of this branch also carries brief 1's R6 fix, which is the one thing left to re-check. |
| `p2p-stack-cycled` | **folded into PR 1; no round of its own** | The diagnostic that names a P2P stack another app is cycling now sits inside `pr/native-aa-wireless` as its last commit. A unit whose WiFi Direct is being switched off and on by another app refuses every `createGroup` and used to say nothing about why; only edges arriving 3 s clear of one of our own P2P requests are attributed, and the receiver's own line is quotaed so it cannot flood an exported log. **Its own verdict needs an affected unit, which the rig is not** — `pr-readiness-round1-brief.md` §4 only checks it does not fire falsely on our own station stand-down. |
| `five-ghz-channel` | **round 2 done — the pin is a plain success in the joined state too; the retry ladder is still unexercised on hardware because this chip has no `-2` case, branch PR-ready** | Round 2 re-ran the matrix with **D-HU joined to `Pegue Cdesta`** (station down at round start — dropped since `p2p-bringup-loop` r1; `allowAutojoin=false` on the saved net and no `adb` verb re-enables it, so the operator re-joined it by one tap in system WiFi settings, the §7a-authorised repair; re-associated at **5260 MHz** ch 52 DFS, not the 5500 §7a records). Same tip `6f1ee214`, APK md5 `5e1c871dcf7b70c46b02e43498a2a955` unchanged. **R0 PASS** (md5 identity gate, no rebuild). **R1 PASS — outcome 1**: 36→5180, 149→5745, automatic→5240, each on the **first** `createGroup` attempt with the station up on 5260 MHz; `createGroup failed` / `retries exhausted` / `already refused` = **0** across the whole round. This chip carries a STA on ch 52 and a P2P GO on ch 36/149 concurrently (MCC), so `wpas_p2p_setup_freqs()` never returns `-2` for it and `cc85d7ab`'s ladder does not engage — **still unmeasured on hardware; the round says so rather than claiming coverage**, it stays on `FiveGhzChannelPolicyTest` / `WifiP2pOperatingChannelPolicyTest`. Automatic pick this time = 5240 MHz (UNII-1, adjacent to but not the station's channel; round 1's was 5805 UNII-3 — the driver just varies, the pin removes the variance). **R2 not run** (pre-registered: only on outcome 2/3). **R3 PASS**: D-POCO joined the pinned 5180 MHz group, listed it in its own scan at 5180 MHz, reached `Incoming connection detected from /192.168.49.60` + `SSL handshake complete`, station associated before/during/after. One benign `MATCH!` (phone BT reconnect, zero churn). Discard checks clean on all 4 captures (`createGroup SUCCESS`=1 each, `p2p-wlan0-0..3` monotonic). New finding, not asked: the station's **Rx rate collapses 433→6 Mbit/s** while a P2P group is up and doesn't recover between runs (single-radio MCC cost; association never drops) — a user projecting while the HU also uses home WiFi sees the home link degraded not dropped. `settings.xml` restored byte-identical (md5 `1ac92489...`), HU left joined to `Pegue Cdesta`, phone radios restored. Edited scripts `five_ghz_matrix.sh` / `five_ghz_r2.sh` (added a station read each). See `five-ghz-channel-round2-results.md`, evidence in `evidence/five-ghz-channel-round2/`. **Nothing queued next — the branch is PR-ready; the ladder's on-hardware behaviour needs a device with single-channel STA/P2P, which this rig is not.** <br><br>**Round 1 outcome, for reference:** Candidate `fork/feat/native-aa-wpp-tcp-and-hfp-link` @ `6f1ee214` (6 commits on `a7076ff4`), APK md5 `5e1c871dcf7b70c46b02e43498a2a955`, `3.3.0`. Candidate-only. Devices: D-HU = MT50 (every run), D-POCO = POCO X3 NFC (**Android 15**, GH 17.5.663204, driver country **CO**) for R2/R4. Results: `five-ghz-channel-round1-results.md`, evidence in `evidence/five-ghz-channel-round1/`. **R0 PASS** — **1113 / 0** exact, `FiveGhzChannelPolicyTest` 7 / `WifiP2pOperatingChannelPolicyTest` 22 / `NativeGroupBandPolicyTest` 26 / `SoftApBandPolicyTest` 12 / `ProjectionQrPolicyTest` 9, DEX symbols present. **R1 PASS — the round**: six HU-only bring-ups, `createGroup SUCCESS`=1 each, `p2p-wlan0-0..5` monotonic. Every pinned channel arrived on exactly its named frequency — **36→5180, 40→5200, 44→5220, 48→5240, 149→5745** (all confirmed by `dumpsys wifip2p` `frequency:` + `channelFrequency=` events, `band=5,freq=5xxx` in the connection stats vs `band=3,freq=0` for automatic). The automatic control rolled onto **5805 MHz** (UNII-3, ch 161) with no `was asked for` suffix — the invisible-channel hazard live. **No channel refused; the retry ladder fired 0 times.** `setGroupOperatingFrequency` works as the commit claims. **R2 PASS both channels** — D-POCO listed `DIRECT-*-Navegadortz2` in its own scan at **5180 MHz** (ch36, -22 dBm) and **5745 MHz** (ch149, -24 dBm), full session to `SSL handshake complete` each, `Incoming connection detected` from `192.168.49.x`. The "one channel joins, the other doesn't" domain-split finding **did not occur** — D-POCO's driver country is CO (same as D-HU) and CO permits the whole 36–165 list including UNII-3; a UNII-3-restricted phone would be needed and none is on this rig. `MATCH! Starting AapService`=1 each (phone's own BT reconnect, zero group churn, benign). **R3 INCONCLUSIVE** — the honest-line path is unreachable: `getSoftApConfiguration()` returns null on this unit (`could not read the current access point configuration: null`), so the app starts the hotspot unconfigured and never logs the channel request or the accepted/refused line; **no false claim made** (it logs "the band is whatever it already had configured, which this app cannot read"), so not the FAIL the rewrite guards against — coverage on `SoftApBandPolicyTest` (12). Control: `cmd wifi start-softap ... -b 5` → SoftAP up at **5745 MHz** (scan-confirmed), but `force-softap-channel enabled 36` is **rejected by this ROM's wifi shell**, so even the privileged path won't pin ch36 here. **R4 INCONCLUSIVE** — Gearhead's `DeepLinkResolver` is exported and **handled** the `androidauto.com/projection/setup` intent (no browser fall-through, `Displayed ... +267ms`), but logged `Experiment DEEP_LINK_ENABLED is disabled. Showing error.` + `QR_CODE_NOT_SUPPORTED` → **the experiment's default is OFF on Gearhead 17.5.663204**, so the branch's QR route is well-formed but inert on this phone; no record written, no session. **R5**: BSSID dump (the `6f1ee214` passenger) validated — the `IPv6 link-local` and `sysfs / ip link` rungs resolve a real EUI-64 MAC on every group while every legacy MAC rung is masked/null; the rewritten "every source has been tried and none named an address" line fired **0 times**, `onGroupInfoAvailable` reports `source=IPv6 link-local` every time. New scripts `five_ghz_matrix.sh`, `five_ghz_r2.sh`. `settings.xml` restored byte-identical (md5 `1ac92489...`), candidate left on D-HU, D-POCO radios restored. Shipping read: R1 proves the P2P half does what it says; the SoftAP and QR halves can't be exercised on this rig but neither regressed anything and both are JVM-covered. |
| `reloaded-wireless-wifidirect` | **round 1 done — fix works on the real head unit; two-phone leg blocked by a phone-side Gearhead race, not by the six commits** | **R0 PASS** (candidate 1049/0, baseline 1006/0, +43). **R1 PASS (route exists)** on both devices — the IPv6 link-local rung resolves an EUI-64 MAC (`DE:B3:88:55:B3:92` on D-MOTO, `E2:32:8F:43:50:0F` on D-HU), and on the phone-as-head-unit it is the **only** rung that resolves anything (every legacy rung masked/SELinux-blocked). **R4 PASS** — D-HU regression: clean Native AA session, BSSID from the new route (`sysfs / ip link` agrees), full handshake, `Incoming connection detected`, ~50 fps `dropped=0`; the reordered chain and the shape check regress nothing. **R2 INCONCLUSIVE** — candidate resolves a real BSSID on every group with **no `masked/empty` abort**, but across 3 attempts the two-phone RFCOMM handoff never completed: Gearhead 17.5 on both phones logs `WIRELESS_SETUP_FAILED_TO_START_NO_HFP_FROM_HU_PRESENCE` because OHU's 15 s poke can't hold the stable HFP link Gearhead's wireless SLC needs between two phones. **R3** (baseline control) hit the exact target abort — `BSSID is still masked/empty (00:00:00:00:00:00) at Type 3 time` — on 1 of 2 attempts; the other got the same `NO_HFP` wall, so the baseline is **as flaky as the candidate** and R2's 0/3 is not a regression. **R5**: HFP-record gate behaves exactly as the brief predicted (skip line on D-HU, absent on D-MOTO); AT responder UNTESTABLE (0 HFP accepts). **R6 INCONCLUSIVE**: insecure record publishes as designed (`9e0db2b1`), same `NO_HFP` wall. **R7**: `status=SUCCESS(0)` observed (`bad30e9d`); no field-2 port, no `hint=`, no version fields. Results: `bssid-round1-results.md`, evidence in `evidence/bssid-round1/`. **Addendum `bssid-round1-results-addendum.md`: Headunit Reloaded V8.2.0 connects two-phone on the SAME rig, logged both sides — its poke is a 30 s held A2DP-source nudge (vs OHU's 15 s HFP poke-and-drop) and it drops its dummy HFP only on `VideoFocus acquired`; it hands the phone the identical BSSID `DE:B3:88:55:B3:92` OHU's new route derives → independent proof the fix is correct.** **Shipping read: fix is sound, R4 proves no real-HU regression; the missing candidate Type-3 run is a rig limit, coverage on `Eui64BssidPolicyTest`.** **The addendum's poke reading is superseded by the decompile: they poke the same HSP-AG record we already try, they release it earlier than we do on success, and their own capture shows the phone's hands-free profile connected and Gearhead dialling before their poke socket opened. The poke is not the variable.** Continued as the `hfp-profile-presence` thread below. |
| `hfp-profile-presence` | **round 2 done — the default alone reaches the gate, opt-out and D-HU regression both clean; branch PR-ready** | Candidate `fork/fix/hfp-profile-presence` @ `ee49a623` (4 commits on `e6b19c3a`), APK md5 `e78b1a39b2cb23ff874f3e364001f0cb`, `3.3.0-beta4`. Baseline: none built (R1 re-uses round 1's key-written-`true` result; R2/R3 are candidate-only controls). Devices: D-MOTO = HU under test, D-POCO = projecting phone (Android **15**, GH 17.5.663204), D-HU = MT50 regression. Results: `hfp-profile-presence-round2-results.md`. Round 1: `hfp-profile-presence-round1-results.md` (mechanism proved 3/3 with the key written by hand). **The question this round answered:** the key was flipped on by default + given a settings row, and the default path had never run. **R0 PASS** — **1071 / 0** exact, `HfpServiceRecordPolicyTest` 11; decoy (`c5609e16`) gone — every decoy-specific DEX symbol 0 (brief's own grep `"AudioSinkDecoy\|Audio Sink"` gives a false 2 on the unrelated `enable-audio-sink` strings). **R1 PASS — 3/3, the point of the round** — key **absent**, nothing written: every run `hands-free service level connection established` = 1 → D-POCO `HeadsetStateMachine` for D-MOTO reaches `Connected` → `NO_HFP_FROM_HU_PRESENCE` = 0 → `Incoming connection detected` → SSL, launch→SSL 11–14 s, one `createGroup SUCCESS` each. Stand-in variant all three runs: `gets the stand-in HFP record, because it advertises no Hands-Free` (D-MOTO's reflective read succeeds — answers R4's first question; the unreadable branch is unexercised here). **No video in any R1 run** — `AapProjectionActivity` pauses ~20–40 ms after resume on a phone-in-portrait acting as head unit (surface mismatch `2237x1080` vs `1080x2400`), Gearhead withholds the stream, audio streams fine the whole ~3 min; unrelated to this change, R1's video coverage rests on R3. First R1 attempt discarded — my harness cut D-POCO's BT before launch (brief §2 says leave it up), phone missed the first group, `recoverNativeGroup` churned it 4×; re-run with round 1's `BT_CYCLE=0` recipe is clean. **R2 PASS** — `native-aa-complete-hfp-slc=false`: `hands-free service level connection established` = 0, `HFP responder active` = 0, failure returns (`NO_HFP_FROM_HU_PRESENCE` ×12, `WIRELESS_SETUP_FAILED` ×12, no session), D-POCO profile reaches `Connecting` then drops, never `Connected`. Group churn (3× `createGroup`) is expected — no session forms by design. **R3 PASS (D-HU regression)** — key absent (new default): `already advertises Hands-Free` = 1 (skip branch), `gets the stand-in HFP record` = 0, `hands-free service level connection established` = 0, `HFP responder active` = 0 — HFP path fully inert on a real head unit; one clean session, `Connection accepted from` → `Incoming connection detected` → SSL (launch→SSL ~10 s), **33 throughput windows all `dropped=0` at 46–51 fps**. **`adapterForService(...) failed` now prints at `D`, not `E`** (`74a70d12` demotes it — round 1's R5 caught it as an error). **R4** — per-capture harvest table in the results file; two greps over the surviving round 1 D-MOTO captures: `audio sink decoy accepted` = **0** (decoy removal provably behaviour-neutral), `could not read radio` = 0 but **not decisive** (the string postdates `c5609e16`). **Carry forward:** `native_wifi_version_exchange` renamed to "Factory-style handshake" in `values/` only (~18 locales still show "Modern handshake (version exchange)"); `CAR.SERVICE: InitialVideoFocus: Disabled by setting` appears on D-POCO in *every* session including the video-carrying R3, so it is not a no-video signal; a phone-as-head-unit cannot carry video from D-MOTO (use D-HU / a landscape tablet). New script `hfp_slc_default_run.sh` (round-1 `hfp_poke_probe.sh` + round-2 greps + `BT_CYCLE` env, default 0). `settings.xml` restored byte-identical on D-MOTO and D-HU, `svc power stayon false`, candidate left on D-MOTO + D-HU, D-POCO on its own build, bonds intact. Nothing queued next. |
| `intercom-native-mode` | **round 3 done; branch PR-ready** | Goal: all Android Auto audio in a helmet intercom paired to the **phone** while OHU runs native mode. Round 3 R5 ran the riding configuration with the audio sink off, took the assistant over the intercom's SCO microphone with the head unit silent, ducked and restored music, and reconnected after a user exit. Candidate `fork/fix/mic-and-vehicle-type` @ `4cc84c22`. Results: `headunit-info-round3-results.md`. Background: `intercom-native-mode-findings.md`. Corrections: `headunit-info-round3-addendum.md`. Serves #818 and, topology permitting, #784. |
| `selfmode-playback-focus` | **round 1 done — the change is inert in Self Mode; safe to ship for Self Mode users; nothing queued** | Candidate `fix/846-audio-focus-auto-trial` @ `abf9067a`, one commit on `967cb41d`. APK md5 `9396fd081c40b4fa4ab47bc3943c9aeb`. Unit D-POCO (`4f4027e9`), Android 15, Gearhead 17.5.663204; BT speaker on the phone for R1/R3. **Build gate PASS**: `PlaybackFocusPolicyTest` **20/0** (4 new cases present by name). **R1 INCONCLUSIVE — the point of the round**: Self Mode *sets up* the AUDIO2/SYSTEM channel (`Media Sink Setup Request: 1 on channel AUDIO2`) but **never sends it a Media Start** — every `Media Start Request` in every run was VIDEO — so `onAudioPlaybackStarted` never runs for an audio channel and the whole diff never executes. `com.andrerinas.headunitrevived` **never calls `requestAudioFocus()`** and never appears in `dumpsys audio`'s focus stack in any sample. `acquiring transient system audio focus` / `leaving system audio focus alone` printed **0×**. Spotify played continuously from ~31 s in to the end of the 4.6-min run; its one pause (before OHU even launched) was **Gearhead's** own AA-startup `USAGE_MEDIA/SPEECH` focus grab, mode-independent, seen on every connect. **R2**: the only protocol focus message was `RELEASE` (0× `GAIN`); the single `AapAudio … Audio Focus Request: stream=3, type=4` line is that release being passed through ungated (same in old and new code). **R3 INCONCLUSIVE** (`playback-focus-mode=2`): byte-for-byte the same picture as R1 — with the code path unreached, NEVER is indistinguishable from AUTO. The matched pair *is* the answer (brief §6/§8). **R4 PASS** (10 min 38 s, mode=0, phone BT off): **49–60 fps, mean 52.9, `dropped=0`** across 126 windows, no disconnect, **0** latch lines, clean discard checks. **R5 PASS**: `playback-focus-self-defeating` **ABSENT** after the full round — the latch never armed, exactly as the announcement-guard reading in brief §3 predicts; no escalation. **Brief errata**: `:5277` was already listening (no manual toggle needed); `Audio Focus Request: GAIN` is *not* "always printed" on this route (0 across 3 sessions); the honoured form `stream=3, type=1` never appeared, only the RELEASE-path `type=4`, so §6's R1-INCONCLUSIVE predicate should exclude that line. New script `hur-wifi-test-scripts/spf_selfmode_run.sh` (its closing fps grep is broken — fps taken post-hoc from the logcat). Settings restored byte-identical; phone BT re-enabled; candidate APK left installed; Gearhead never force-stopped. See `selfmode-playback-focus-round1-results.md`. |
| `selfmode-keyboard-viewmode` | **round 2 done — both fixes verified on hardware, all six runs PASS, ready for PR; nothing queued** | Candidate `fix/883-self-mode-call-raise` @ `6411eaef`, built from source. APK md5 `35dc0b84142577fedd14adb91273e725` (3.3.0-beta3, versionCode 102). Unit D-POCO. **Build gate PASS**: 863 tests / 0 failures, `VideoFocusReleasePolicyTest` (8) + `PictureCredibilityPolicyTest` (9) both green. **R1 PASS — the point**: keyboard held ~23 s and was ended by the operator, not a bounce. `SurfaceCallback: onSurfaceDestroyed.` present (reachability guard), `the surface went away 1352ms after a touch - holding video focus` once, **zero** `VIDEO Video Focus Notification` between that line and dismiss, **zero** `Asked by projected IME to detach` in the whole capture. **R2 PASS — the positive control**: Home press still releases (`VIDEO Video Focus Notification` same millisecond as `onSurfaceDestroyed`), **no** `holding video focus` line, `onResume` → `First frame rendered` = **359 ms** (< 500). The gate is narrow. **R3 PASS — the cost**: `onResume` → `keyframe decoded - the picture is repaired` = **1643 ms** (brief expected 1.7-3.2 s). The new credible-picture gate does not stand down on the 191 ms gray P-frame — it drives the focus cycle, which pulls an 8200-byte on-demand IDR. **R4 PASS — all three transitions**: `frames are rendering but no keyframe` once per switch, then the cycle + keyframe, gray→picture **1648 / 1634 / 1555 ms** (round 1: 11.0 / ≥32.4 / 66.5 s). **R5 PASS**: 10.6-min mock-drive + media session, 127 throughput windows all `dropped=0`, 49-60 fps, **zero** new lines, zero disconnects. **R5d PASS (added on request)**: R5 re-run with WiFi **off** (offline dummy-VPN route, not loopback) and the new real-road Marinilla mock route — 10.4 min continuous, 124 windows all `dropped=0`, 49-59 fps, zero gate lines, zero disconnects, one stable session. Earlier attempts R5b/R5c were INCONCLUSIVE (round 2 teardown had left the dev head-unit server down, plus a mid-run WiFi toggle and two competing mock-drive processes — no code fault). **R6 PASS**: 108 s keyboard hold with typing — session never dropped (no `ByeBye`, no re-handshake, no overlay, no unacked stall), typing worked, return 1615 ms, then 25 min more of healthy session (21 natural-GOP keyframes, 308 windows, `dropped=0`). **Two deviations, both after all data was captured**: R4 had no live capture (every decisive line recovered from `-b all` ring buffer with timestamps, run still decisive); and the teardown script force-stopped Gearhead against brief §3 — **D-POCO's dev head-unit server on `:5277` is now down and needs a manual "Start head unit server" toggle in AA Developer settings before the next Self Mode round on that unit**. `settings.xml` restored byte-identical (`diff` clean). See `selfmode-keyboard-viewmode-round2-results.md`. Round 1: `selfmode-keyboard-viewmode-round1-results.md`. |
| `aap-reorg` | **round 3 done — re-run against the new tip `e4d4bf70`; all four PASS and round 2's projection-view-rebuild finding is fixed** | **Round 3** re-ran the round 2 brief (all four runs) against `fix/head-unit-server-silence-and-log-attribution` @ `e4d4bf70` (one commit past round 2's `6479a374`: *"Video: a directly uploaded YUV frame now counts as a drawn frame"*, `GlProjectionView.kt` +5). APK md5 `edfc19475083814825a0402ab03a6199`. **R0 PASS** 780/0 delta 0 (adds no tests). **R1 PASS** — symbol audit byte-identical to round 2 (14 `Java_` symbols, no stale prefix); `e4d4bf70` touches no native code. **R2 PASS** — hardware HEVC Native AA session, ~48 fps `c2.unisoc.hevc.decoder`, one group (`p2p-wlan0-0`), one SSL handshake, projected-widget tap toggles Spotify `PLAYING`↔`PAUSED` deterministically, 0 `UnsatisfiedLinkError` in 497k lines. **R3 PASS — and the fix is confirmed**: bundled FFmpeg HEVC engages (`Configuring bundled FFmpeg HEVC decoder for 1920x1080` + `thread count: 6`, `first direct YUV420 upload` present), but the projection-view rebuild loop round 2 flagged is **gone** — round 2 logged the engagement line 9× (initial + 4 stall-driven rebuilds in 30 s); round 3 logs it **once**, with **0** `Rebuilding projection view` / `Recreating projection view` / `Display stall`, steady ~47 fps `codec=ffmpeg-hevc`, 0 `rendered=0` windows, 0 MediaCodec fallback, 0 `UnsatisfiedLinkError` in 204k lines. `settings.xml` restored md5-identical. Also corrected: round 2's doc mislabels `view-mode=2` as TEXTURE — it is GLES, which is exactly the path `e4d4bf70` fixes. See `aap-reorg-round3-results.md`. Nothing queued next — the branch is ready for its PR.<br><br>Round 2, for reference: all four runs PASS against `6479a374`, APK md5 `c365fd12a088983e1f40b6a8b3038d43`. **R0 PASS**: `assembleGithubDebug` clean (round 1's `:app:kaptGithubDebugKotlin` failure on the `modes.native.CredentialField` stub is gone), **780/0 exact** — the on-record `58802778` count, delta 0. **R1 PASS — the symbol audit**: `libusbhelper.so` carries exactly 10 `Java_…_connection_usb_UsbNative_*` symbols (matching `UsbNative.kt`'s 10 `external fun`s), `libhur_soft_hevc.so` exactly 4 `Java_…_decoder_video_FfmpegHevcDecoder_00024Companion_*` (matching the companion's 4), across all 4 ABIs; self-check for the pre-fix `connection_UsbNative` / pre-move `decoder_FfmpegHevcDecoder` strings → 0 files in the whole APK; 14 distinct `Java_` symbols total, no others. **R2 PASS** (after the operator re-paired the phone to the HU mid-round — the bond was genuinely gone, not a ROM quirk): full Native AA session, one `createGroup SUCCESS`, one SSL handshake, single `p2p-wlan0-0`, 38 throughput windows all `rendered>0` at ~52 fps `c2.unisoc.hevc.decoder`, `dropped=0` throughout, projected-widget tap (`input tap 272 657`) → Spotify `PLAYING` (touch channel end-to-end, since `TouchCoordinateMapper` moved), zero `UnsatisfiedLinkError` in a 490k-line capture. **R3 PASS** (`force-software-decoding=true`, attempt 2 — attempt 1 hit the rig's known poke/reconnect variance, phone bound to the OEM `FX Plus` radio): `Configuring bundled FFmpeg HEVC decoder for 1920x1080` + `FFmpeg HEVC decoder thread count: 6` + `Bundled FFmpeg HEVC decoder initialized`, 45 throughput windows all `rendered>0` (~48 fps, `codec=ffmpeg-hevc`), **zero `UnsatisfiedLinkError`**, no `Configuring decoder:` MediaCodec fallback, no FFmpeg failure line — the `_00024Companion_` JNI path confirmed at runtime. `settings.xml` restored byte-identical after each run. **One finding for the coding session, not a relocation bug:** on the software-decode path only, `AapProjectionActivity` logged `Recreating projection view due to settings change` 4× in the session's first 30 s (restarting the decoder each time) then settled — absent on the hardware path. See `aap-reorg-round2-results.md`. Nothing queued next — the branch is ready for its PR. |
| `selfmode-playstore-route` | **round 1 done — hypothesis refuted for the 17.4+ route, confirmed for the legacy route; `playstoreDebug` compiles** | Candidate-only `fix/head-unit-server-silence-and-log-attribution` @ `58802778` (3 commits on `d1fef63a`). APK md5 github `7d793c0cd716c9b984554bb5796684f4` / playstore `47eed0db99e78d66a9fb8ebed44933bb`. R0-R8 + one added control, **no FAIL**. **R0 PASS**: `assembleGithubDebug` clean, **`assemblePlaystoreDebug` clean — first build ever** (`BUILD SUCCESSFUL in 3m 54s`, full native + kapt + Kotlin; the workflow "cannot compile" comment is stale), `testGithubDebugUnitTest` **780/0** exact. **R1 PASS** (D-POCO, github, offline, VPN): `tun established` → full session 54-59 fps held. **R2 PASS — hypothesis refuted** (D-POCO, github, offline, **intent route = no VPN**): full session anyway, connect→SSL 0.525 s, `Head unit connected` ×1, no #897 signature. **R3 PASS — the reporter's exact config** (D-POCO, playstore, fully offline, auto-start, AA 17.5): `Device is offline and VPN is not available in this build` **and a full session**, 53-54 fps held, playstore/debug banner. **R4 PASS** (playstore, online) / **R5 PASS** (playstore, joined AP with no internet) — neither offline line, session holds. So on the 17.4+ route **no network of any kind is needed** and the dummy VPN does nothing the hypothesis claimed. **R6 PASS** (D-MOTO, playstore, offline, AA upgraded 17.3→17.5 by pulling the splits off D-POCO): reproduces R3, 51-52 fps. Needed an operator to enable D-MOTO's dev "Start head unit server" (`:5277`) by hand — it had never been on. **R7 PASS** (D-MOTO, github, offline): `tun established` → session, 52 fps. **R8 PASS (recorded)** (D-HU, playstore, offline, **legacy** 5288 route, AA 17.3): Gearhead's `WirelessFSM` starts (`Launch projection 127.0.0.1 5288`) but the connection never lands, `nothing connected within 30000ms`, **no session**. **R8c** (added control, D-HU, **github**, offline, legacy — same rig/round/route): VPN comes up, `WirelessServer: Incoming connection detected` at +2.6 s, session forms. **So the Play Store build cannot do offline Self Mode on the legacy (< 17.4) route** — that half of the hypothesis holds; it's just the wrong route for #897, whose log says `AA 17.4+ detected` and whose exact config (R3) works here. The reporter's peer-goes-silent-after-accepting signature was reproduced in **zero** runs. **R9** (added post-round: forget the head unit in AA, retry offline on D-POCO): session still forms, **but the fresh-connection capture exposes the likely #897 mechanism** — Gearhead runs its First-Run Experience (`CAR.FRX.CHECKS | isFrxRequired=true`), D-POCO passes `cakewalkMinimumRequirements` / `isSupportedOsVersion` / `appsUpToDate` so `shouldRunLegacyFrx=false` and the connection proceeds; a phone that *fails* one of those checks (old Android or outdated Google/Maps/TTS/Search app, plausible on a SIM-less tablet) falls to the **legacy FRX**, an interactive account/permissions flow that needs a network — offline + legacy-FRX-required → `:car` stalls on a setup screen → peer goes silent. Not proven (couldn't make D-POCO fail the checks) but it's the first log-backed candidate and it points the fix at the phone (update AA + Google apps, or finish AA first-run once with a network). Devices restored (settings byte-identical, radios on, `:5277` still listening on both phones, Gearhead never force-stopped); candidate left installed on all three. New script `hur-wifi-test-scripts/build_hur_playstore.sh`. `exporterLogLevel` is `INFO` (no `DEBUG` default). See `selfmode-playstore-route-round1-results.md`. Nothing queued next — `playstoreDebug` can go in CI; for #897, ask the reporter for a phone-side logcat (`CAR.FRX.CHECKS` / `GH.*` verbose) from a failed attempt to confirm the legacy-FRX path. |
| `post-beta1-self-mode` | **round 2 done, rebased candidate re-verified, ships** | Re-run of the round 1 brief against the rebased candidate `82df5d33` (was `8bc6fcce`; rebased onto current `main` `78689a96`, now 2 commits, same fix content, 8 files +203/-68) and baseline `78689a96`. APK md5 `452dc20f7e8ac5a14d03d8203efff181` / `825b36fd5d91663c60c3e03512a067b9`. R0-R8 all done, no FAIL. **R0 PASS**: candidate 770/0, baseline 765/0, exact match, delta +5 = `SelfLaunchTimeoutPolicyTest`. **R1 PASS** (point, half 1): candidate spawns zero app-owned `logcat` on all three devices (0 probe lines, `MainActivity` resumes ~1 s, full Self Mode session on all three); baseline reproduces the `LogAccessDialog` on D-HU (app-uid `logcat` pid spawned, dialog held the foreground the entire poll, unattended, did not auto-dismiss, `MainActivity` never resumed). D-POCO baseline spawns `logcat` silently with no consent UI (API 35), D-MOTO baseline denies it silently (API 34, `READ_LOGS` off), both pre-registered report-not-fail. **R2 PASS**: candidate `HUR_Log` capture still works, 81 KB file with `LogExporter: session |`; the session formed while the consent dialog was still up, so `onCreate` was not blocked (probe gone). This ROM re-fires the LogAccess dialog ~1/s and kills the capture each time, so a valid file needs a brief Allow-tap window then leaving `logcat` alone (new section 7a candidate). **R3 PASS** (point, half 2) on D-HU + D-MOTO: legacy Self Mode forms in 1352 ms / 2033 ms, no teardown in the first 30 s, no `nothing connected within`, no "Failed to start Android Auto" toast, no defect-3 line. **R4 INCONCLUSIVE** for the defect-2 mechanism: baseline connects on both devices (1244 ms / 1443 ms), always beating the old 2500 ms watchdog, so the disconnect-on-timeout never fires here; coverage rests on `SelfLaunchTimeoutPolicyTest` (R0 +5) and the candidate R3/R7 behaviour. **R5 PASS (candidate) / INCONCLUSIVE (baseline)**: every device offline for the whole round (only an IMS-only LTE PDN on two, no default network), so the dummy VPN (`excludeSelf=false`) came up on R3/R8 too; candidate held the tun the full 90 s with no `releasing the dummy VPN`, baseline never released it either (same reason as R4). **R6 PASS both arms**: `CarErrorDisplay` = 0 on D-MOTO on candidate and baseline; D-MOTO Self Mode is healthy. **R7 PASS**, exact spec: `SelfMode: nothing connected within 30000ms of the launch` at +30.09 s (not 2.5 s), `AA permissions request took 316 ms` then `AA's permissions are in order; the launch timed out for another reason.` (not `could not be started`), no teardown. Needed a `pm disable-user` at +3 s / `pm enable` at +27 s to hold "nothing connects for 30 s" on this build (legacy Self Mode reconnects ~1 s after a single Gearhead force-stop); 3 earlier attempts documented. **R8 PASS**: D-POCO 17.4+ route forms and holds 90 s, no teardown/timeout; one transient first-attempt failure (`Headunit Server ... is NOT running`) that self-recovers via `NetworkDiscovery` ~9 s later, same as round 1. Build version bumped to `3.3.0-beta2 (101)`. `wifi-connection-mode` unmoved on all three. PC thermal: turbo disabled (`no_turbo=1`), builds capped `taskset -c 0,2,4,6 --max-workers=4`, peak package 88-89 C, zero throttle, no power-cut (vs 2 hard-power-cuts on the pre-round-1 attempt); a corrupt Gradle transforms cache had to be cleared first. No new scripts added. See `post-beta1-self-mode-round2-results.md`. Nothing queued next; defect 2's hardware confirmation stays deferred to a slower setup or the reporter's next log.<br><br>Round 1, for reference: same verdicts against `8bc6fcce`. See `post-beta1-self-mode-round1-results.md`. |
| `post-beta1-latency-instruments` | **round 2 done — instruments verified on hardware, latency keys buy nothing, ships** | Candidate `fix/post-beta1-latency-instruments` @ `3200f004`, candidate-only (no baseline — instruments do not exist on `main`). APK md5 `7340fbdce0de981911ff2d481dcfc12e`. R0-R8 done, no FAIL. **R0 PASS**: `assembleGithubDebug` clean, **816 tests / 0 failures** exact; round 1's `InboundRateMonitorTest` `1000L` fix is folded in. **R1 PASS all three** (5-min sessions, swipe-animated): `optionalKeys=none`, `presented=`+`decodeLatency=` on every window (0 unreadable), ≥8 inbound-rate lines with non-zero `video=`, 0 `rendered=0` windows, `dropped`=0. `decodeLatency` median D-HU 20 ms / D-POCO 16 ms / D-MOTO 18 ms. **R2 PASS all three — the A/B**: low-latency on, D-HU tier `realtime` `[priority, operating-rate]`, both qti devices tier `vendor` `[vendor.qti-ext-dec-low-latency.enable, priority, operating-rate]`, **0 rejections on any component**, and **`decodeLatency` did not move on any device** (20→20 / 16→16 / 18→18 ms median; p95-field 26→26 / 19→19 / 22→21). The keys are accepted and ignored — keep them (R4 shows they cost nothing), don't default on. **R3 UNTESTABLE**: no `.mtk.` component (`c2.unisoc.avc.decoder` D-HU, `c2.qti.avc.decoder` D-POCO+D-MOTO). **R4 PASS all three**: `force-software-decoding=true`+`software-video-decoder=0` → `c2.android.avc.decoder`, `preferHardware=false`, `optionalKeys=realtime`, **no `Decoder rejected optionalKeys=realtime`** — on-hardware confirmation `KEY_PRIORITY`/`KEY_OPERATING_RATE` can't fail a configure. **R5 PASS (D-POCO)**: 5 Home cycles, `Decoder rejected` = 0 anywhere; the surface-loss path *was* hit 3× (`Decoder start aborted: the surface went away mid-configure`, `:1776` not the ladder's `:1722`) each ~60 ms after `onSurfaceDestroyed`, clean reconfigure each time. **R6 PASS (D-MOTO)**: `headunit://exit` → `Hiding reconnecting overlay - the session ended` ×1, `frames resumed` = 0; `feed queue resized` never fired, `queue=30 frames` everywhere. **R7 PASS on the three stated conditions** (verified from the retained capture segments): 25-min VERBOSE capture rolled ≥3 segments, kept the newest 2 (one full 8 MiB `SEGMENT_BYTES` segment with both roll markers), tail at 14:08:03 vs 14:07:24 end (within 2 min), joined 8.5 MB with `--- continued from the previous log file ---`, **0 `produced 0 bytes`**, no `APPLOG_FILE` switch, `log-source` still 0 — a roll was not mistaken for a refused capture. **But the in-app "Export Logs" button ANR'd the app** (`saveLogToPublicFile` runs `logcat -d`+`waitFor()` on the main thread in its ring-buffer fallback, `LogExporter.kt:318-324`; the ROM's LogAccess gate hangs it) — reported as a deviation + finding, not a FAIL. **R8 PASS all three**: `This unit has no 5 GHz band` absent from every R1 capture. **Setup:** added `round-post-beta1-latency-instruments/run_selfmode.sh` + `parse_throughput.py`. **D-POCO's 17.4+ path breaks if you force-stop Gearhead** — it kills `DeveloperHeadUnitNetworkService` on `:5277` and only the AA Developer-settings "Start head unit server" toggle brings it back (6 failed attempts, operator re-enabled by hand). `auto-start-self-mode=true` + an explicit `ACTION_START_SELF_MODE` double-launches and the 17.4+ second launch trips `quiesceWirelessForWiredSession` on the loopback socket. Settings restored on all three, `wifi-connection-mode` unmoved (3/1/2). See `post-beta1-latency-instruments-round2-results.md`. Nothing queued next — the branch is ready for a PR; the export-ANR finding is a separate `main` fix (move `logcat -d` off the UI thread).<br><br>Round 2 brief, for reference: `post-beta1-latency-instruments-round2-brief.md`. Round 1 (BLOCKED after R0): `post-beta1-latency-instruments-round1-results.md`. |
| `log-and-selfmode-fixes` | **round 1 done — all three commits verified on hardware, ships** | Candidate-only, `fork/fix/log-export-anr-and-self-mode-double-launch` @ `809adff3` (three commits on `4e5be786`). APK md5 `ca5dfeedee4b577e7cc042c90180a6cc`. R0-R5, no FAIL. **R0 PASS**: `assembleGithubDebug` clean, **775 tests / 0 failures**, `SelfLaunchCoalescePolicyTest` 5/5. **R1 PASS (D-HU), a real before/after**: the pre-round beta2 build had `READ_LOGS: granted=true` on D-HU (the cause of the `LogAccessDialog` in the two prior threads); after installing `809adff3` the permission is not on the package at all (`requested permissions:` empty for it, `pm grant` → `has not requested permission`), **0 `LogAccessDialog`** in a 479k-line capture, Self Mode session held 180 s, and the `HUR_Log` opens with the `LogExporter: session |` banner and carries the app's own Java **and** native lines (`c2.unisoc.hevc.decoder` ×44, `CCodec`, `Codec2Client`) with no `produced 0 bytes` and no `APPLOG_FILE` switch. **R2 PASS (D-HU)**: "Export Logs" tapped on both the live-capture path and the `logcat -d` ring-buffer path — "Logs Exported" dialog within ~1 s each, `NOT_RESPONDING` flag 0 for 13 s, **no `ANR in` / `Input dispatching timed out`**. The 10 s `Process.destroy()` bound is **INCONCLUSIVE by construction** (the permission that made `logcat -d` hang is gone), coverage on code review. **R3 PASS (D-POCO), the point of the round**: `auto-start` + explicit `ACTION_START_SELF_MODE` on the 17.4+ route → `SelfMode: AA 17.4+ detected` then `SelfMode: a launch is already in flight; ignoring this request rather than starting a second one`, one session held ~110 s at 52-56 fps, and **0 each** of `All launchers failed` / `nothing connected within 10000ms` / `USB session established while wireless mode` / `Self Mode disconnected` / `quiesceWirelessForWiredSession` — the `post-beta1-latency-instruments` round-2 collapse does not reproduce. **R4 PASS (D-MOTO)**: legacy single launch forms and holds, then `headunit://exit` → `Self Mode disconnected. Not restarting.` → relaunch → a second session forms with `a launch is already` count 0, i.e. `clearLaunchInFlight()` did not strand the feature. **R5 PASS (D-HU)**: legacy double-launch is harmless — the legacy launcher sequence runs in ~40 ms so `launchInFlight` clears before an adb duplicate lands, the second launch's broadcast fallback "had no issues" so `All launchers failed` never fires, session holds; the coalesce *line* is a 17.4+-route phenomenon (that route's single launcher takes 2-3 s), not a legacy one. **Setup**: added `round-log-and-selfmode-fixes/run_selfmode_full.sh` (unfiltered capture, the latency round's `run_selfmode.sh` filter hides `LogAccessDialogActivity` and native tags). Candidate `809adff3` left installed on all three; settings restored and verified identical to backup on all three; D-POCO `:5277` never force-stopped and still listening. See `log-and-selfmode-fixes-round1-results.md` and `logcat-access-android13-findings.md`. Nothing queued next — ready for the PR. |
| `headunit-info` | **rounds 1-3 done; branch PR-ready** | Rounds 1-2 established that the microphone service and the motorcycle claim are both required. Round 3 was the first forbidden from forgetting the car: it inserted a motorcycle record, formed a session with no `No audio/mic` and moved the recorder to the phone, and the riding configuration survived a disconnect and reconnect. Its R3 FAIL was a briefing error, corrected in the addendum: the phone keeps two lookups and `headUnitCarInfoInternal` is the Bluetooth-keyed one, which does not carry the vehicle type. Candidate `fork/fix/mic-and-vehicle-type` @ `4cc84c22` (5 commits, 906 tests). Results: `headunit-info-round1-results.md`, `-round2-`, `-round3-`. Corrections: `headunit-info-round3-addendum.md`. |
| `self-mode-call-foreground` | **round 3 done — the fix works, ships** | R0-R4 done, R5 not run (deferred). **R0 PASS**: 784/784 exact match to prediction (765+13+6), md5s confirmed, `SYSTEM_ALERT_WINDOW=allow`. **R1, the point of the round, PASS on four independent live-call cycles, not just one**: every raise landed on **attempt 1 of 3** (never needed 2 or 3), Dialer `START u0` → our `onResume` averaged **2076 ms** (2124/2092/2038/2050 ms across the four), `dropped=0` throughout, `Configuring decoder:` never re-fired, and Android 15 genuinely honoured the overlay trampoline every time (confirmed via our own `START u0` carrying uid 10268, not just the "succeeded" log line). One call never covered the projection at all (Dialer's `InCallActivity` never launched) — a call-answer-path variability, not a defect. Also found: `call raise finished` never logs on a first-attempt success, since `onResume()`'s own `closeCallRaiseEpisode()` silently tears down the pending tick before the loggable `DONE` branch can run — a real, reproducible quirk of the log, not a functional gap. **R2 PASS**: outgoing call, two cover/raise cycles, 1927 ms and 2094 ms, both attempt 1, direction-independence confirmed. **R3 PASS**: the no-call half's Home press didn't actually background the activity on this phone (known rig-quirk-adjacent behavior) so that half's zero is uninformative; the call half's Home press did genuinely pause it (`onUserLeaveHint`) and produced zero raise lines despite the live call — the meaningful result. **R4 PASS**: baseline with the setting off stays covered for the whole call (`topResumedActivity` confirmed live on Dialer throughout), self-returns at hangup with no `START u0` for our package — same mechanism round 1 described, though the magnitude (2158 ms) is much larger than round 1's ~17 ms. **R5 not run** (deferred by mutual agreement — needs a live in-app mode switch without restarting the process, plus the UNISOC rig's own Android Auto reconnecting to this phone; R1-R4 already answered the round's central question). See `self-mode-call-foreground-round3-results.md`. Nothing queued next for this thread. |
| `video-feed-backpressure` | **round 1 done — core mechanism confirmed, one new finding needs a look before shipping** | R0-R7 all done. **R0 PASS**: 749/749 exact, `VideoFeedThrottlePolicyTest`=8, `KeyframeCycleEscalationPolicyTest`=43. **R1 (the run the round depended on) PASS**: baseline `bf389ccf` at hold=40ms reproduced the reporter's defect on the first attempt — `dropped` non-zero in all ~123 windows (30-164), plus a screenshot and the vendor decoder's own `an unintentional loss of picture occures!` line corroborating it visually. **R2, the point of the round, PASS on all three conditions**: `dropped=0` in every one of the candidate's windows (vs R1's consistently non-zero), 539 pacing log lines (12,817 events incl. suffixes), `enqueueWait` rising 2443ms→~4470ms then holding steady, `rendered`/`fed` matched throughout including a transient ~55s dip (source-side, not the fix costing frames), zero `sync_stall`. **R3, the number the round exists to produce: zero audio underruns per minute at every tested hold (10/25/40/100ms)** — but the operator watching the rig reported real, felt touch/control input lag at hold=25 and hold=40, a cost the only instrumented metric (audio underruns) completely misses; 10ms was confirmed inert as predicted (the second control). **R4 surfaced more than the predicted INCONCLUSIVE**: the codec genuinely wedged 16 times under fault injection (contrary to the brief's assumption that no hold value could produce a wedge), the 3-cycle escalation budget was exhausted and never refunded because the injector kept firing, and the picture stayed black for 54 consecutive 5-second windows (4.5+ minutes), unrecovered when the capture was stopped — `dropped` stayed 0 throughout, so the specific budget-expiry-shed signal this run was built to check never fired, plausibly because `sync_stall` recovery wins the race first. Reported as INCONCLUSIVE on the stated condition, with the sustained black screen flagged as its own open finding. **R5 PASS**: teardown at 345ms after the disconnect intent, zero ANR, no stale feed-thread lines from a torn-down session bleeding into a fresh one. **R6, the H.265 regression guard, PASS**: `enqueueWait=0ms` and `dropped=0` in all 122 windows, median 52fps, completely undisturbed (one uncorroborated transient visual artifact noted, no counter moved). **R7**: the escalation refund line never fired, a valid non-appearance since pacing prevented any drops for it to act on (879 pacing events, 0 drops, 15:01 continuous). One discard-rule hit and clean re-run: R3's hold=100 step was contaminated by an accidental mid-run disconnect, re-run clean. See `video-feed-backpressure-round1-results.md`. Nothing queued next for this thread — the R4 black-screen interaction and the R3 input-lag/underrun mismatch are both worth a look before this ships. |
| `wifi-launcher-parity` | **round 1 done — 2 confirmed regressions, both fixed on the candidate** | R1 and R4 are both **A=FAIL, B=PASS, C=PASS**: confirmed regressions in the 3.3.0 refactor, both fixed by `fix/wifi-launcher-parity`. **R1 (the point of the round)**: Native AA over the head-unit hotspot never got its credentials on arm A (`SoftApCredentials: SUCCESS` ×12, never delivered, two 60s handshake timeouts); arms B and C both completed the full handshake (B in 5.27s, C in 8.49s), and arm C added a new guard skipping the poke when an active BT hands-free link would be disrupted. **R4**: a forced mode-restart on arm A called `VpnControl.stopVpn()` twice, the second time with no `releasing the dummy VPN (owner=...)` line — an unowned teardown call, exactly the brief's FAIL signal; B and C both show exactly one, correctly owned, call. **R2 INCONCLUSIVE on all three arms** (phone's Bluetooth never re-cycled naturally after `headunit://disconnect` within the observed window on any arm — a consistent rig reachability limit, so the re-arm defect itself remains hardware-untested; route to a JVM test). **R3/R6/R8 UNTESTABLE on all three arms**, a phone-side constant: Gearhead 17.4.663004+ disabled the broadcast Wireless Helper's connect trigger depends on (`[[project_gearhead_17_4_broadcast_disabled]]`), and this phone runs 17.5.663204 — confirmed every other piece (hotspot, `WirelessServer` bind, phone joining the network) worked. **R5 UNTESTABLE as pre-registered** (AA 17.5 ≥ 17.4 gate). **R7**: INCONCLUSIVE-by-construction on arm A per the brief's own rule (R1 FAILed there; the server was never bound at all, not just late), **PASS on B and C** (bind at arm time, before the poke, no late-bind line). **R0**: arm B 738/738 clean; **arms A and C both have a real, pre-existing unit-test compile failure** (`UserExitHotspotPolicyTest.kt:154`, a type mismatch present since `e8fe4611` itself and untouched by the candidate's own commits) that blocks getting real suite counts on either — one-line fix needed before this goes upstream. See `wifi-launcher-parity-round1-results.md`. Nothing queued next for this thread. |
| `self-mode-bt-audio` | **round 1 done, answered** | R0-R1 both **PASS**, plus an added **R3 positive control, PASS**. **R0**: first-ever compile of `ada271e7` on this rig, clean; 738/738 unit tests; Android Auto reads `17.5.663204-release`. **R1 (the point of the round): Bluetooth, in all four arms** — the active A2DP device, a live named `AUDIO_DEVICE_OUT_BLUETOOTH_A2DP` output thread and a `state:started` Spotify track agree in every arm; the per-device Media audio policy (`A2DP=100`) never moved; zero audio `Media Start Request` anywhere; car mode first appears in arm B exactly as predicted (`enableCarMode(0)` firing from service creation with no session) and the announced service set is byte-identical between arms C and D (`enable-audio-sink` off vs on) — `Media Sink Setup Request: 3 VIDEO + 1 AUDIO2` both times, no AUDIO/AUDIO1 either way, confirming the switch is disconnected in Self Mode exactly as the branch predicted. **R2 not run** (brief's own gate: only if B/C/D failed). **R3 (added post-round, user-requested)**: toggling the paired device's own **Media audio** switch off in the phone's system Bluetooth settings reproduces the reporter's exact silence — `A2DP` drops to `0`, `mActiveDevice` goes `null`, the Bluetooth output thread vanishes from the flinger dump — while HUR's own session (AAP handshake, video throughput, `AapMediaPlayback` status relay) stays completely undisturbed throughout, confirmed from the continuous capture; flipping it back on fully recovers. No scriptable path exists for that toggle (`cmd bluetooth_manager` has no per-device connection-policy verb, `adb root` refused), done by hand both directions. New script `hur-wifi-test-scripts/set_pref.sh` added for phone-side (non-rooted, `run-as`) settings writes — the existing `set_hu_pref.sh`/`set_hu_prefs.sh` are hardcoded to the head-unit rig's rooted shell. See `self-mode-bt-audio-round1-results.md`. Answer is in hand for the reporter; nothing queued next for this thread. |
| `connection-failure-banner` | **round 4 done — ships** | R0-R4 all done, no FAIL. **R0 PASS**: 698/698, exact match, unchanged from round 3. **R1 PASS, the point of the round**: the hotspot condition raised (`SoftApCredentials: The access point on ... is up`), the banner came up on relaunch (`showing the connection issue banner for HOTSPOT_CONFIG_UNREADABLE`), and the removed notification/toast stayed removed — zero notifications beyond the ongoing foreground-service one over a 60s window, checked both via `dumpsys notification` and a shade screenshot, and no toast in the first 15s with `show-toast-messages=true`. **R2 INCONCLUSIVE**, the brief's own pre-registered fallback: with the head unit's own location services off (the device that actually reads its BSSID, not the phone's — the brief's wording was ambiguous, traced to `WifiDirectManager.kt:443-445`), the BSSID fallback chain still resolved a usable address and a full session formed both times (once discarded for a self-wake `createGroup SUCCESS`=2, once clean); `BSSID is still masked/empty` never fired, so the abort R2 is built to exercise was never reached on this rig. **R3 PASS**, both band hints verbatim on screen with no scrolling, found by searching a substring of the hint text itself rather than the row's label (the row's own search doesn't surface its adjacent `InfoBanner`). **R4 PASS**: Advanced tab confirms the full order Wireless Mode → Network transport → WiFi Direct band → its note → Use the upper 5 GHz range → Bluetooth Adapter → Manual Secondary Radio → Modern handshake (version exchange), every adjacency screenshotted; Basic tab still has the band selector and note; 2.4 GHz only correctly hides "Use the upper 5 GHz range" and Auto brings it back; backed out without saving and `wifi-direct-band` was unchanged on disk. See `connection-failure-banner-round4-results.md`. Nothing queued next for this thread — ready for the PR.<br><br>Round 3, for reference: **R0-R8 all PASS**, no FAIL, MIXED or INCONCLUSIVE. **R0**: 698/698 exact, all six suite counts exact. **R2, the headline**: the seeded stamp survived a publish with both hotspot fields set, where round 2 read `0` on the identical state. **R3**: the dismissal path, never tested before, and the banner came back. **R4/R5**: the BSSID half's first hardware run and its control. **R7a-e**: the trimmed note verbatim, Experimental gone from the transport but kept on the Auto-Enable Hotspot dialog, the band selector in Basic, Static BSSID gated across five mode combinations, and the remedy tap landing on "Static BSSID (MAC Address)". **R8a-c**: Auto to 5 GHz, 2.4-only to 2412 MHz, and the old `debug-force-p2p-band-24` flag migrating to `wifi-direct-band=2`. Two arms stayed untested as the brief predicted: the hotspot disproof and the pre-Q channel ladder. See `connection-failure-banner-round3-results.md`. |
| `release-test` | **round 1 done — Part A out of scope this round, Part B complete** | Part A was pulled from scope mid-round by direction (both branches already passed their own Native AA rounds individually; the merge-specific A1 check was judged covered by A0's unit gate this time) — only **A0 ran: PASS**, `afd8b7ca`'s first-ever compile clean, 669/669 unit tests, all eleven named suites present at the exact predicted counts. A1-A7 not run, no verdict, still open for a future round. **Part B (PR #845) complete, B0-B6 all done.** **B0 PASS**: first-ever compile/test run for this PR, 565/565. **B1 PASS**: side-by-side install works, store build unaffected; the locale finding (Dev suffix missing in `de`/`ru`/`ja`/`es`/`it`/`pl`/~25 others) confirmed via the compiled APK's own resource table. **B2 — the headline result, worse than the brief's framing**: not just "can't share port 5288" but **starting the second build kills whichever session the first build already had**, reproduced both launch orders, neither build can hold a session while both run. **B3 PASS** (free, from B2's own capture): error+throwable lines get a real stack trace, error-only lines get no stray blank line. **B4 PASS** all 3 runs: tunnel builds in 7-8ms every time, the PR's target race (`Attaching the inbound STREAM...`) never fired on this rig/network. **B5 PASS, finding refuted**: a failed connection does not poison the next attempt with a stale `lastQuality` — verified with a genuine pre-`STATUS_OK` failure followed by a clean second attempt. **B6 PASS**: exactly one sweep-summary line, numbers partition correctly (nothing on this subnet answers on 5277, so the review's double-counting scenario wasn't exercised). See `release-test-round1-results.md`. Also surfaced: PR845's `.dev`/diagnostics build variant defaults `log-source` to `APPLOG_FILE`, so **zero** lines from it reach logcat unless set explicitly — a logcat-only capture of this PR's `.dev` build is silently empty. |
| `wire-corruption-escalation` | **round 1 done; branch since extended to `cd603ac0` (4 more commits, JVM-covered, exercised as the baseline of the `render-side-concealment` round); going to PR** | R0-R5 all **PASS** (R3's budget-exhaustion half and R4's "holding the cycle" guard are honest **INCONCLUSIVE**/untested, not failures). **R0**: first-ever compile of `96eefddb`, clean; 571/571 tests, `AapMessageFramingTest` 4/4. **R1 (the point of the round): truncation-to-repair in 2682 ms**, with `cycling video focus (1/3)` in the chain — matches the reporter's one escalatable event (2.78 s) far more than the 11-27 s the non-escalating truncations took. **R2 (baseline A/B): zero `cycling video focus` across ~20 minutes**; only one usable repair sample (not the planned median-of-three, screen-state candidate stall) but it was **68.65 s — over 25x slower than R1** — confirming the mechanism is absent on `main`. **R3**: both faults that landed cycled correctly (`1/3` then `2/3`, 2673 ms / 2687 ms, 7m14s apart) but only 2 of 4 landed, so budget exhaustion (`3/3 spent`, "no cycle available") went untested. **R4**: 30-fault 1-in-3 storm lands in ~1.5-1.7 s on this screen, faster than the escalation's own 2 s timer, so only 1 cycle ever fires and lands clean (budget-spent-to-repair 1182 ms, `dropped=0`) — the "holding the cycle" guard against a mid-storm bad keyframe was never reached on this rig/screen combination; first attempt discarded for `createGroup SUCCESS`=4 (self-inflicted poke churn before the real session), re-run clean and corroborated the discarded one exactly. **R5**: five zeroes over 10 minutes at steady 45-50fps, and the new `96eefddb` session banner confirmed present both via the capture-file path and, separately, via the ring-buffer path after deleting the capture file. Round also hit two head-unit reboots (one spontaneous, one from running on bare PC USB power at 1% battery) — neither corrupted a reported run. See `wire-corruption-escalation-round1-results.md`. `64630ce7` (the actual cause fix) remains untestable by any injector mode, per the brief, and is covered by JVM tests plus the reporter's next log. |
| `render-side-concealment` | **round 1 done, all PASS, going to PR** | C0-C6 all **PASS**. **C0**: first-ever compile of both `03df5890` and `cd603ac0`, clean; 605/605 tests, `CorruptionConcealmentPolicyTest` 16/16, `AuditRecoveryPolicyTest` 10/10, `KeyframeCycleEscalationPolicyTest` 40/40. **C1 (the point of the round): picture restored 2683ms after a truncated frame** — 1ms from round 1's 2682ms repair on the same fault shape, `concealed=63` then `49` in the two windows spanning the freeze, `rendered=` never 0, zero kill lines. **C2**: the previously-wedging holed-fragment fault was discarded before the codec, `Configuring decoder` stayed at 1 for the whole session, restored in 2651ms. **C3 (negative control)**: zero `holding`/`discarding`/`DELTA_CHANGED` on the undetectable hole, as predicted — its own decoder-stall/restart is the brief's pre-registered blind-spot reaction, not a freeze. Needed a longer window than planned: the idle-screen candidate trickle stalled at 63/87 candidates for over a minute before the fault fired at 260s in, extending observation on the same live session rather than restarting. **C4**: the orphan fragment, restored 2659ms. **C5 (the storm)**: all 30 faults landed in 1.615s, exactly one `holding`/`restored` pair (2666ms), throughput back to 50+fps in the next window — cap-expiry correctly INCONCLUSIVE (storm beat the cap), covered by JVM tests. **C6 (clean control)**: first attempt discarded for 5x `createGroup SUCCESS` in 4.5 minutes — the self-wake poke-loop churn (#760) this repo's own `CLAUDE.md` already documents from log analysis, unrelated to this candidate; re-run clean, 121 throughput windows over 10 minutes, `concealed=0` in every one, `Configuring decoder`=1, zero kill lines. See `render-side-concealment-round1-results.md`. Nothing queued next for this thread. |
| `session-vpn-lever` | **round 2 done — ships** | R0/R1/R2/R3 all **PASS**. `82814ec0`'s direct-descriptor-close fix works: round 1's defect does not reproduce. **R2 (the point of the round): `tun0` went to "does not exist" 10s after a scripted `headunit://disconnect`, with `pidof` unchanged (28321) before and after** — proof the process stayed alive while the interface actually came down, not a `force-stop` masquerading as a teardown. Exactly one `Dummy VPN stopped` for that teardown, no leftover `DummyVpnService` `ServiceRecord`. **R3: a genuine second `tun established` after a phone-side Bluetooth re-arm** (new P2P interface index), ruling out `startVpn()`'s idempotence guard silently absorbing the call — then a second clean teardown, same pid, `Dummy VPN stopped` totalling exactly 2 for the round. R0: 612/612 tests, `DummyVpnPolicyTest` 7/7, clean compile. R1 unchanged from round 1's PASS. `onRevoke()` stays out of scope for hardware testing (routed to code review, no second VPN app on this rig to trigger it); round 1's toggle-off control correctly not re-run. Three benign `MATCH! Starting AapService` lines appeared (phone's own BT reconnect) with zero unexplained `createGroup SUCCESS` churn — consistent with the `native-aa-5288` thread's existing characterization of that signature. See `session-vpn-lever-round2-results.md`. Nothing queued next for this thread; round 1's unscored `ACTION_START_WIRELESS_SCAN` cascade finding (native-aa-5288-round1-results.md territory) remains open for whoever next touches Native AA. |
| `native-aa-5288` | **round 1 done, all PASS** | R0-R4 all **PASS**. R0: first-ever compile of `c2efedda`/`e9f5d2b6`, clean; 565/565 tests, `WirelessServerRestartPolicyTest` 12/12. R1 (clean start): bind in 6ms, single group. **R2 (the point of the round): held port 5288, released it, repair chain fired in full — 1.819s from `found port 5288 unbound` to `Incoming connection detected`.** **R3 (the run that mattered most): port held 5 minutes straight, rebuilds bounded to exactly 3 per 60s window (min gap 15.1s, floor 10s), correct INFO backoff after the third, and — the real subject — `createGroup SUCCESS!` count stayed at 1 and `p2p-wlan0-N` stayed at one interface (`8`) the whole 5 minutes despite constant repair attempts.** R4 (10-min clean control): zero rebuild/backoff/error/null-handshake lines; 4 credential deliveries all agreeing on SSID+BSSID (torn-read race itself didn't fire — pre-registered INCONCLUSIVE on that half, as expected). One Setup-notes finding worth a look: every run produced a benign `MATCH! Starting AapService` from the phone's own Bluetooth reconnect (not a poke) with zero group churn each time — `TESTING-TEMPLATE.md`'s discard rule may need narrowing to "a second `createGroup SUCCESS`" specifically. See `native-aa-5288-round1-results.md`. Nothing queued next for this thread. |
| `media-gap-instrument` | **round 2 done, going to PR, nothing queued** | These four commits now ride on `fix/session-lifecycle-and-diagnostics` @ `6bd336ed`, rebased onto the new `main` with identical content; `fix/media-gap-instrument-and-attribution` @ `f48baee7` is behind `main` and superseded. Two log-line commits were added after round 2 from round 1's remaining findings (the station-state line now prints on the unjoined arm too, and service discovery says when the audio sink is switched off). Neither is queued for a round of its own; both print at session start, and `session-vpn-lever-round1-brief.md` §5 carries the two greps. **Caveat on round 2's R2**: it passed, but the picture ran at 39-46fps for the whole window, so there were zero qualifying gaps and the 85% ceiling was short-circuited rather than evaluated. Its only proof remains the unit-test replay of round 1's measured numbers, and round 2 shows the trickle is not reproducible on demand. Round 2 otherwise confirmed the ceiling fix: **R0 PASS** (594/594, `LinkGapMonitorTest`=17, `StationCoexistencePolicyTest`=8, all named tests present). **R2 PASS — the point of the round**: `inbound video quiet` stayed 0 on a stationary, untouched Google Maps screen for 3 minutes, throughput steady at 39-46fps the whole time. **R1 PASS**, all four zeroes over a clean 10-minute session, same live session reused for R2. **R4 PASS**, `Disconnecting the other network` gone (0), the coexistence line still fires descriptively. R3/R5/the unjoined arm were not re-run, per the brief. See `media-gap-instrument-round2-results.md`. Round 1: R0 PASS (588/588), R1 PASS (four zeroes, 45-55 fps), R3 PASS after a stale `enable-audio-sink=false` was found and fixed, R4 joined arm PASS (unjoined arm UNTESTABLE on this rig), R5 INCONCLUSIVE at 1-in-300. R2 FAIL was that round's finding and drove the round-2 design change. See `media-gap-instrument-round1-results.md`. |
| `native-gps-forwarding` | round 1 done, plus a user-requested R8 | R0-R7: R3 (the point of the round) PASSED: `sentOnWire=true` on the first try, no LOCATION drop after channel open. R4 (cadence count) is INCONCLUSIVE — OS-level mock-injection artifact on this rig, not a code defect, confirmed with a positive control. R5 (backstop/re-arm) PASSED. R2/R6/R7 all PASSED (R2 flipped per the brief's own contingency, since this rig had a real live GPS fix). **R8 (added post-round): phone-side GPS spoof still pulls the projected Google Maps view away from the car's real position, with screenshot evidence** — but the branch's own send path was verified correct and unbroken throughout (303 clean fixes, no re-priming); `com.google.android.apps.maps` on the phone was found holding direct listeners on the phone's own (spoofed) location provider, independent of the AA sensor channel. See `native-gps-forwarding-round1-results.md`. Worth a dedicated follow-up round on the end-to-end map-source question; nothing else queued. |
| `release-next` | **round 6 done, awaiting next brief** | R1 (reserved cycle) **FAIL**: zero holds, never reached a 3rd check, recovery still via the `sync_stall` ladder at 77.6s — source-traced cause is a decoder-rebuild re-arm racing a 60s-from-check (not 60s-from-cycle) NUDGE retry. R2 (control) zero-counts **PASS** but its own recovery-gap vs R1 is void (6.3x apart) and it surfaced a **604.6s continuous black screen**, invisible to the app's own audit the whole time (mode-2 corruption never trips the reader-stage signal) — round 5's R3 outlier (253.9s) recurring at 2.4x the magnitude, worth its own round. R3 (isolated-drop guard) **INCONCLUSIVE**, 0/2 faults in 11.4min even after the brief's own authorized remedy (panned the map). R4 clean control **PASS** on all three jobs including the free discovery-loop check (0 on a plain native session). R6 read-desync **PASS**. See `release-next-round6-results.md`. |
| `video-pipeline-stack` | **acted on** | Round 2 validated the decoder-wedge fix: **R0 PASS** (454/454, first ever compile), **R2 PASS** (both faults recovered in ~0.55s via `cycling video focus` → `keyframe reached the codec`, `rendered=` never 0 against round 1's 90+s), **R4 PASS** (control clean, one codec init across 10 corrupted frames, zero starvation lines), **R6 PASS** (10 clean minutes, zero `DELTA_CHANGED`). **R3 INCONCLUSIVE** — 0 faults in 5 min at rate=20, which the brief predicted and which is now fixed at the source: the injector prints its candidate count per fault and a summary every 15s, so a zero-fault run says so during the run. **R5** answered the mode-4 probe: inert, and with the session's only working lever replaced it reproduced round 1's wedge in full (`rendered=0` for 108+s, unrecovered at capture end) — the probe has been removed. The whole five-branch stack has since been compacted into **one branch, `fix/video-pipeline` @ `d43112e2`, four commits** (457 tests), and the five old refs were **deleted** — anything still on one is orphaned and needs a fetch and reset onto the new branch, not a pull. Two tags keep this document's SHAs resolvable: `round2-validated` (`8f0beab1`, the APK measured below) and `stack-pre-compaction` (`b1057bf8`, all sixteen commits). **Superseded — this thread's work now lives on `fix/video-stack` and is tested under the `release-next` thread.** R2's validation was 2 injected faults; the `release-next` round 1 R6 run drew 90 at the same setting and reopened the wedge, so do not quote R2 as proof without its fault count. See `video-pipeline-stack-round2-results.md`. |
| `video-dropped-frame-keyframe` | **queued** (fix already in PR #826) | `video-dropped-frame-keyframe-round7-brief.md` — R1 is a desk check with no rig time; R2 is optional |
| `audio-focus` | **queued** | `audio-focus-round11-brief.md` |
| `usb-device-diagnostics` | **Round 5 done (`usb-device-diagnostics-round5-results.md`) — the Direct Boot fix works; branch PR-ready.** Candidate `feat/usb-device-list-diagnostics` @ `843b78d7` (3 commits on `main` @ `2f07eeec`), APK md5 `d9172c5a1de6e972ef6deb1e0888c8b9`, 984/0. **R0-R5 all PASS, R6 not run (skipped by direction).** R1: on a locked cold boot `App started in Direct Boot mode (locked)` prints, `UsbAttachedActivity.onCreate` runs to completion and `Ignored blacklisted USB device`, **zero crashes** — where the pre-fix build crashed 3x at `App.kt:46` (round 4's R5 FAIL, reproduced again here as the before-state, `evidence/usb-device-diagnostics-round5/prefix-build-3-crashes-attempt2.log`). R2: `User unlocked: credential storage is available, applying settings` fires **from the same pid** (4093) and the device-protected mirror actually gains the stale `auto-start-wifi-ssid` probe value across the reboot — receiver fired *and* body ran, fix is finished, no `BootCompleteReceiver` fallback needed. R3: the `directBootAware` accessory switch still runs while locked (`connectAndSwitch | Result: true`, Poco → `2D00`), session correctly deferred (`Usb in accessory mode, but the user has not unlocked yet`), no `AapService` before unlock. R4: unlocked path unchanged — `log-level=0` honored (3293 D + 2736 V lines), full USB projection session 47-52 fps `dropped=0`, **foreground notification present** (`channel=headunit_service_v2`, the channels-from-`getSystemService` proof). R5: the in-service attach path now logs `UsbLauncherListener.onUsbAttach | Ignoring USB device attached in service (VID: 6353): rejected: blacklisted by the user` (round 4 got `accepted: ADB` there). **Setup notes matter for next time:** the moto shipped with a pre-fix `9b85bcb4`-lineage build that passed a filter-xmltree + DEX-string check but lacked the `initUnlockedOnce`/`userUnlockedReceiver` symbols — verify those, not the filter, to confirm `843b78d7`; and the moto's `-b main` buffer is 256 KiB with `persist.logd.size*` refused, so it rolls in seconds — attempt 3 lost the whole window, attempt 4 worked only after suppressing ROM spam with `persist.log.tag` props (set back to empty afterward). `settings.xml` restored byte-identical; R7 USB grants (`4EE1`/`2D00`) untouched; candidate left installed; no new script. | **Nothing queued.** Branch is PR-ready. R6 (the WiFi/BT locked-defer lines) stays hardware-unverified but is a plain `!isLocked` guard covered by the USB-path arms. If a round is ever wanted for it, set the `persist.log.tag` spam filter before the reboot (see round 5 results §Setup notes) and revert after.<br><br>**Round 4 (`usb-device-diagnostics-round4-results.md`), candidate `9b85bcb4`:** R0-R4 + R6 PASS, R7 INCONCLUSIVE, **R5 FAIL** — the BFU `App.onCreate` crash, now fixed on `843b78d7` and verified in round 5. Blacklist validated end to end (name-key across 6 VID:PIDs incl. accessory mode, start button refuses blacklisted rows, un-blacklist restores the session, `ad082fff` Samsung CDC-ACM arm accepted). `UsbLauncherListener.kt:25` logging `matchReason()` not the blacklist verdict — also fixed and verified in round 5 (R5). Evidence: `evidence/usb-device-diagnostics-round4/`. Earlier rounds 1-3 in `archive/` territory / their own results files. |
| `usb-session-teardown` | **round 1 done, answered — attributed to Android Auto, not us** | Diagnosis round, no candidate fix, `0ff9e620`. **R0 PASS**: build/tests clean, both USB devices identified (flash-drive-via-hub never registered as host-connected on this phone, substituted a Logitech mouse), Gearhead log-level raise verified (7291 lines by pid). **R1 PASS**: an established session survives 3 minutes fully untouched (195s clean window, steady 50-52fps, `dropped=0`) — the reporter's correlation is not an artifact of general instability. **R2/R3, the point of the round, both FAIL (reproduction) — and R3 is the discriminator that didn't discriminate**: detaching either the audio dongle (R2, 535ms USB-to-EOF) or a plain HID mouse (R3, 1162ms) kills the session identically. Both times, Android Auto's own `CAR.SERVICE.USBMON.LITE` logs the physical OTG port going `connected=false`, and `GH.DHUService` (Android Auto's own Headunit Server) dies (EPIPE / SocketException) the same millisecond our side sees `Connection closed (EOF)`. `ACTION_USB_ACCESSORY_DETACHED` fired 0 times in either run (rules out H3, our own code), and no `ConnectivityService`/`NetworkAgent` loss appears in either window (rules out H2). **Attribution: H1** — Android Auto's own Headunit Server tears down the Self Mode loopback session on any physical USB port event, audio or not; not a bug in Open Headunit. R4/R5/R6 not run (battery-limited; R6's question is answered incidentally by the 0-count above). **Also surfaced, unscored**: every cold launch this round got stuck on "Android is starting" for up to ~2.5 minutes, hitting Android Auto's own 5s `FIRST_ACTIVITY_LAUNCHED` timeout — a separate, likely significant reliability issue worth its own round on a fresh phone. See `usb-session-teardown-round1-results.md`. Nothing queued next for this thread. |
| `audio-start-and-teardown` | **round 1 done — the audio half is validated and the WiFi half has been dropped from the branch** | R0 **PASS**: baseline 765/765 tests, candidate 782/782 (+17 exactly matching the three new suites). R1 **INCONCLUSIVE, pre-registered**: this rig's AudioFlinger never emits an underrun line on either arm (0/10 and 0/6 real starts) despite AudioFlinger itself being active — the A/B is unanswerable here. Strong secondary evidence though: baseline crashed with the exact predicted `Error during audio track cleanup`/`InterruptedException` twice independently; candidate's pre-roll fired cleanly on every single start (`8192 frames banked, target 9600, 4-7ms`). R2 **PASS**: pre-roll target correctly caps at ¾ buffer capacity on the small buffer (2886 vs 9600), scales down, starts in ≤7ms, zero queue-full. R3 **PASS**: baseline mean 2562ms (matches reporter's ~2500ms, one crash-shortened 291ms outlier), candidate mean 379ms/max 629ms, zero crashes. **R5 — a real finding, not the predicted clean PASS: neither arm ever forms a session in 90s when the poke races the service's own bring-up at launch**, on any of 5 attempts per arm. Candidate is measurably more bounded (deterministic 2 groups/5 deliveries every time vs. baseline's variable 2-3 groups/6-9 deliveries) and eliminates the millisecond-scale collision (baseline collides in 11ms; candidate's fold defers it), but the folded request still tears down and rebuilds the group ~2.5s later and **no session ever forms either way** — exactly the "fold with no session forming afterwards" failure mode the brief itself flagged as mattering more than the raw counts. **R6 FAIL, blocks the branch**: cycle 1 clean, but cycle 2 wedged for 9+ minutes — `WifiDirectManager`'s own join watchdog fired 4 times on its 60s cycle and deferred recovery every time because it saw a Bluetooth handshake "in flight", never recognizing the handshake was *repeating* (every ~43s, fully completing "on the Bluetooth side" each time) rather than progressing to a real WiFi join; only a force-stop + relaunch recovered it. R7 **PASS**: zero `inbound audio/video/link quiet` across a 12-minute session with pause/resume/skip, single group throughout (the Assistant-session sub-step didn't fire — see below). R8 **PASS**: all five required zeroes, one group, 660 consecutive `PLAYING` status packets over the full 10 minutes. See `audio-start-and-teardown-round1-results.md`. **The WiFi Direct commit has since been dropped and R5/R6 no longer apply to anything on this branch.** The branch is now `455e76e0`, two commits, `WifiDirectManager.kt` identical to `main`, 778 tests (782 less `NativeGroupBringUpPolicyTest`'s 4). The measured tip is tag `audio-round1-validated` (`3d3fe465`). What the two runs found is kept here because both outlive the commit: **R5** showed the fold was right and the re-run was not — `WifiDirectManager.kt:1211-1212` ran the folded request by calling `startNativeAaQuietHost()` again, tearing down and rebuilding a 2.5 s old group nobody had joined, so the fold deferred the second group rather than removing it; whenever that work resumes, the change is to drop a folded request whose bring-up already produced a group, which is not the forbidden group reuse (that covers reconnects where the peer's WPS registrar is already desynced). **R6 was never a regression from this branch at all**: `WifiDirectManager.kt:203` in `ea7aa7e0` carries the identical deferral, every window in `NativeHandoffPolicy` is bounded per attempt and nothing bounds the sequence, so a handshake repeating every ~43 s re-stamps the state at each 60 s tick and defers recovery forever, and `MAX_CONSECUTIVE_HANDSHAKE_FAILURES` misses it because those handshakes succeed on the Bluetooth side. Its own branch off `main` and its own round, whenever WiFi Direct is picked back up. **Also surfaced**: this rig has no speaker/3.5mm output at all (every "confirm audible" check in the brief had to route to a scriptable proxy instead); media keys (both phone- and head-unit-relayed) reliably failed to open/resume an AAP audio channel here, only a direct tap on the AA media widget's on-screen play/pause toggle worked — worth folding into `TESTING-TEMPLATE.md` §7a; and the scripted `CarKeyReceiver` Assistant trigger (`com.microntek.irkeyDown`/`irkeyUp`) never reached the app despite both documented preconditions holding — broadcasts were enqueued by the system but never handled, likely because the receiver is only dynamically registered with no manifest entry. **This blocks `mic-uplink-round1-brief.md`'s M1**, which depends on the identical trigger and is that round's own point-of-the-round run — needs a fix or a different trigger mechanism before that round can proceed. |
| `mic-uplink` | **round 3 done — M6a is fixed, branch unblocked, ships** | R0-R4 all **PASS**. **R0**: first-ever compile of `dcef0500`, clean; 836/836 tests exactly as predicted, `ForegroundServiceTypePolicyTest`=8. **R1, the point of the round: 5/5 background-started cycles clean** — zero `SecurityException`/`ForegroundServiceStartNotAllowedException`, every cycle reached the foreground with the base type mask (`0x12`, no microphone claimed at start), `MATCH!`→first-frame 6.2-10.3s. Round 1's M6a FAIL is gone. The brief's own literal recipe (`am force-stop` before cycling Bluetooth) turned out undoable on this rig — force-stop blocks manifest receiver delivery until an explicit relaunch, same as `TESTING-TEMPLATE.md` §7a already documents and round 1's own brief already worked around; substituted that same working recipe (explicit launch once, then `headunit://exit` between cycles) for all five. **R2 PASS**: the microphone still opens on a background-started session — three captures inside one Gemini multi-turn session, claim line landing after `Mic request: open: true` every time, all real non-zero audio. **R3 PASS**: zero `could not drop the microphone foreground-service type` anywhere, 3 claims against 1 assistant session (one claim per capture, as predicted), service still foreground with the base mask after. **R4 PASS after a methodology fix**: the documented package-scoped `appops set $PKG RECORD_AUDIO ignore` didn't actually block capture while the service was foregrounded (a UID-scoped `Uid mode: foreground` override was winning) — real audio got through twice before this was caught; adding `appops set --uid $PKG RECORD_AUDIO ignore` fixed it, and all 4 subsequent requests declined cleanly with `code -3` (not `-6` — permission check precedes the foreground claim, as designed). See `mic-uplink-round3-results.md`. **The branch is unblocked and ready to ship** — M6a, the sole blocker since round 1, is resolved and verified 5/5 on real hardware. Two `TESTING-TEMPLATE.md` §7a candidates surfaced this round: R1's force-stop-vs-`headunit://exit` distinction, and R4's package-scoped-vs-UID-scoped `appops` distinction.<br><br>Round 2, for reference: ran on a Motorola with Gemini and validated LTE, replacing the POCO whose Gemini backend never connected. G1/M1/M3/M4 PASS, M2 the headline (a 48kHz-under-16kHz-announcement mismatch reliably broke transcription), M5 PASS on the head-unit side. M6a remained unresolved and continued blocking the branch regardless of round 2's results (a phone swap can't change a throw in `AapService.onCreate()`). See `mic-uplink-round2-results.md`.<br><br>Round 1, for reference: M0 PASS (first-ever compile, both arms exact). M1-M5 UNTESTABLE, pre-registered fallback — all three trigger routes reached the app and opened a real Voice Session, but the phone's Gemini backend never requested the microphone (`Cannot connect to Gemini`), a phone-side account/backend dependency unrelated to the code. **M6a FAIL, 5/5 cycles — the blocker this whole thread existed to resolve**: a real background restart via Bluetooth `ACL_CONNECTED` threw `SecurityException: Starting FGS with type microphone ... the app must be in the eligible state/exemptions` every time despite `RECORD_AUDIO` granted, and `onCreate()`'s `stopSelf()` didn't actually stop `onStartCommand()` from forming a full unpromoted session anyway. M6b/M7 PASS, unrelated to the block. See `mic-uplink-round1-results.md`. |
| `discovery-socket-leak` | answered at round 7 | nothing queued; awaiting a PR |
| `video-black-after-background` | closed at round 8 | shipped in PR #826 |
| `link-stall-periodic-scan` | **round 5 done — round 4 still queued** | Round 5 tested whether a WiFi station scan blanks the P2P radio. **The brief was revised mid-round** (withdrew `wifi_scan_always_available` as a no-op while WiFi is on; redefined R3 from "suppress scanning" to "characterize this unit's real cadence against AOSP's 20/40/80/160s schedule"). **R0-R4 all PASS.** R0's unit-test gate found and fixed a real, pre-existing, scope-unrelated bug (`P2pOperatingChannelPolicy.frequencyMhzFor(14)` missing the 2484 MHz special case; one-line fix, local commit `307d85f2`, not yet pushed), then 552/552. **R1 (unassociated): zero stalls > 1.2s over 733s** — the brief's pre-registered "mechanism absent" outcome, scan events sparse/irregular (7 events, 27-160s gaps). **R2 (associated to 5GHz): also zero stalls**, scan rate roughly halved vs R1 (3/665s vs 7/733s) — confirms the directional claim with no stall difference on either arm. **R3 original (`wifi_scan_always_available=0`): no suppressive effect** — independently confirms the brief's own revision. **R3 redefined (15-min characterization + display off/on probe): the pure AOSP schedule only partially fits** (two clean post-toggle steps, 19.8s/40.2s, then breaks pattern) — **but a real, named third scanner explains the cadence far better**: `com.google.android.gms`'s `NetworkLocationScanner` (work-sourced to `com.unisoc.phone`) correlates within 30-200ms of 9 of 14 scan events. The brief's own requested `dumpsys wifi` `WifiConnectivityManager` localLog instrumentation is empty on this build — dead on this hardware. **R4: both lifecycle fixes confirmed working** — re-arm after a clean user exit forms a full second session with no join-watchdog teardown, and the manual poke button now re-arms closed listeners and successfully pokes (`Native AA listeners are closed — re-arming before the poke.` → `Successfully poked`), where before the fix it did nothing. Per the brief's own honest limit (§2): **none of this refutes the theory for the reporters' MediaTek silicon** — this UNISOC/Android-14 rig cannot carry #839/#824's fault at all on timescale grounds, but it newly shows a specific non-AOSP location-services scanner is live and worth checking for on the reporters' side. See `link-stall-periodic-scan-round5-results.md`. **Round 4 (`link-stall-periodic-scan-round4-brief.md`, 2.4GHz band + constrained memory profile) remains queued**, now the weaker of the two remaining hypotheses. |
| `media-key-routing` | closed | merged upstream |
| `external-bt-zbt` / `zlink-wpp-channel` | **coding-session task queued, fold moriceh's TCP-link findings into `feat/external-bt-zbt-probe`** | Re-port onto the native-AA branch is **done**: the branch is `b64912805`, 8 commits over `main`, carrying `ZbtByteChannel` / `ZbtFraming` / `ZbtMessages` / `ZbtProbe` / `ZbtAaCarrier` / `HandshakeLink` / `ExternalBtTransportPolicy` (the old plan `external-bt-zbt-on-native-aa-plan.md` is superseded). New input 2026-09-06: Andre forwarded a reporter mail pointing at a third-party fork, `moriceh/open-headunit` @ `e724e171`, that has this working on a Xtrons unit. Its TCP link was derived from a PCAP and matches our disassembly byte for byte (endpoint `127.0.0.1:3152`, 16-byte header, `RequestInit` `0x101` with `enable_type=2`, `0x105` as the raw byte channel), so the protocol is settled. Four changes to make, two from his fork and two follow-ons: a speak-first fallback when the module reports no presence, Type 4 / Type 1 retransmits while the phone is silent, daemon reachability as a real input to the Native compatibility check (so a reachable module is no longer told "cannot work", while unreachable ones stay denied), and `rmnet*` excluded from soft-AP interface selection. Full comparison, his file:line refs, our file paths and the ANR hazard in change 3: `external-bt-tcp-link-findings.md`. Not a hardware round, the coding session executes it; the rig has none of the external-BT markers and cannot exercise the route, only the negative ANR check after change 3. |
| `qf001-firmware-teardown` | answered | — |
| `headunit-reloaded-decompile` | answered | PC-only, no brief. Competing app's video decoder is independently written and behind ours (H.264-only, no HEVC, hardcoded buffer size) — nothing copied, nothing worth adopting. Its Native AA wireless path (fake-HFP AT-command values, P2P/BSSID fallback chain) matches this project's current source closely enough to look copied. See `headunit-reloaded-decompile-findings.md`. |
| `gemini-research-video-optimization` | answered | PC-only, no brief. Fact-check of a user-run Gemini research transcript on low-RAM optimization and decoder artifacts. Its headline "cycle AAP Video Focus to force an IDR" idea is already built and hardware-validated (`triggerFocusCycleRecovery`/`KeyframeCycleEscalationPolicy`), more conservatively than proposed; most other "fixes" already exist in `main`. Two open, untested items: `KEY_LOW_LATENCY`, `AudioTrack` low-latency mode. See `gemini-research-video-optimization-findings.md`. |
| `logcat-access-android13` | answered | PC-only research. The `LogAccessDialog` seen in `post-beta1-self-mode` and the export ANR in `post-beta1-latency-instruments` both trace to `f98de46b` (#887, shipped in `v.3.3.0-beta1`) requesting `READ_LOGS`. Verified against AOSP `android14` logd + `LogcatManagerService`: a granted `READ_LOGS` makes the app a privileged logd reader and forces every capture through the consent gate (dialog when foreground, silent own-UID downgrade when a background service); nothing suppresses it for a normal-UID app. Ungranted, logd serves the app's own UID immediately with no dialog, which is the 3.2.x behaviour and still catches every line the app process writes, Java and native. Fix on `fork/fix/log-export-anr-and-self-mode-double-launch`: `809adff3` scopes the manifest permission to `maxSdkVersion="32"`, `a5cd6fc8` moves the export dump off the main thread behind a 10 s bound. See `logcat-access-android13-findings.md` and the `log-and-selfmode-fixes` thread. |
| `native-aa-poke-hardening` | answered | — |
| `video-latency` | answered at round 1 | — |
| `hotspot-unreadable-config` | closed | the round this branch was created for |
| `p2p-bringup-loop` | **round 1 done — regression-clean on both live edits, ships; nothing queued** | Candidate `fork/fix/907-p2p-channel-request-wedge` @ `ef5080cd` vs baseline `origin/main` @ `a211dd48`. APK md5 `2b25adcf423d9bf6570dff63cd823781` / `c6af71a6a663222546d9a447b5d00069`. **Build gate PASS**: 873/0, `P2pStateChangePolicyTest` 8/0, `ConnectionIssueBannerPolicyTest` 29/0 with both named tests. **Premise checks: `no band request below Android 10` and `no answer in ` = 0 in all 14 captures** — the §0 framing holds, the pre-Q ladder is dead code here. **R1 PASS** both arms, four §5 counts identical (1 group / 1 `Attempting` / 0 `auto-starting` / 1 `state=2`). **R2 PASS** both arms: 5 connect/user-exit cycles → 5 groups → 5 sessions, counts identical, the 2 s gate never swallowed a cycle (cycle re-arm via HU-side `svc bluetooth disable` self-revert, same process throughout so `lastP2pRequestAtMs` is live). **R3 (highest-value) PASS** both arms: a real 10 s WiFi toggle cleared the latches — `P2P enabled, auto-starting Native AA quiet host` at +2 ms of `state=2`, group in 600 ms, no stuck latch, WiFi returned first try. **R4 PASS**: bare-launch `auto-starting WiFi Direct visibility` = 0 on **both** arms (rig brings the group up via the direct `WifiLauncherHelper.start()` path, receiver `makeVisible()` gated identically on A and B — not a regression, brief's bare-launch expectation is wrong for this rig); a toggle variant shows arm B fires the line on a real P2P re-enable. **R5 PASS (B) / control (A)**: `No usable access point after` = 1 (A, `reportBudgetExhaustedOnce`) vs 3 (B, `reportNoAccessPoint`, 30/90/151 s cooldown); `connection-issue-hotspot-off` non-zero long on B, absent on A; `HOTSPOT_NOT_RUNNING` banner on screen on B (screenshot). **R6 INCONCLUSIVE** as pre-registered: the rig *can* raise a SoftAP (`cmd wifi start-softap` → wlan2 @ 5745 MHz) but the app can't read its name and `clear(HOTSPOT_NOT_RUNNING)` only fires on a nameable/joinable AP by design — retire path unreachable here, covered by the JVM tests. Deviations: phone re-paired by hand (POCO X3 NFC, HU had been bonded to an absent Motorola); phone airplane-mode doesn't hold so phone-BT-off was the HU-first lever; `headunit://disconnect` doesn't re-arm mode 3 on this build. The HU dropped its `Pegue Cdesta` station association during R3's `svc wifi` cycles and had not re-joined by round end — next round should re-verify. `settings.xml` restored byte-identical; candidate left installed; no new scripts. Brief: `p2p-bringup-loop-round1-brief.md`. Results: `p2p-bringup-loop-round1-results.md`. |
| `wpp-over-tcp` | **done, PR-ready at `aa54e6e9`** | **Coding session, after round 5:** the ping count is explained. Gearhead's own WPP dispatcher lists `WifiPingRequest` under `Unexpected message type`, next to `WifiInfoRequest`, `WifiVersionResponse` and `WifiConnectStatus`, so pings are the phone's to send and the head unit's to answer; a head unit that pinged would be sending something the build logs and discards. `17.5.663204` sends none over TCP and abandons the channel ~10 s in, which is harmless because projection is a **separate connection on 5288** (`WppTcpServer` accepts on 5299, `WirelessServer` on 5288, within ~1 s of each other; two rounds have called them one connection). `ddf8b198` stays: answer-only is the correct shape for a build that does ping, and initiating is contraindicated. `aa54e6e9` is a comment-only commit recording that, so branch is **7 commits**, 986/0, no behaviour change since the round-5 APK. PR body drafted at `~/projects/ohu-project/ohu-fixes-handoff/wpp-over-tcp-pr-body.md`. No further round needed. **Round 5 outcome:** Candidate `fork/fix/wpp-over-tcp` @ `ddf8b198` (six commits on `2f07eeec`, no rewrite), APK md5 `0d72f3a85f8bcd6f08afd0031e5eb82e`. **R0 PASS** 986/0, `WppHandshakeSessionTest` 30 / `WppEndpointPolicyTest` 6, exact. **R1 PASS — round 4's open regression check is closed clean**: WiFi Direct first, phone forgotten, session forms on **both** the connect and the reconnect and the phone logs `Trying to start WPP on TCP` **zero** times (round 4's contamination announces itself in that line); `advertising WPP over TCP` = 0, the withhold line carries its new "forget this head unit on the phone" tail. **R2 PASS — the point of the round**: `a62cc22b` works, the hotspot exchange now reaches `WppTcpServer: handshake complete; projection session is up` then `holding the control channel open for the session`, on `Type 7` with the `WifiConnectStatus` (Type 6) absorbed after; projection 45–47 fps `dropped=0` across 35 windows (round 4: 50–51; the gap is MT50 variance — R1's WiFi Direct session sat at 46 too, and fps was flat across the channel's open and close). **R3 PASS both reconnects**: `Connection accepted from` = 0, `handshake complete` reached, relaunch→`Incoming` **6.5 s / 6.1 s** (round 4: 7.4 / 5.5), no loop, no brick. **The ping count is `0`** on all three hotspot sessions: this Gearhead (`17.5.663204`) does **not** ping the WPP control channel — it opens the socket, waits a fixed ~10.1 s for an unsolicited first message from the head unit, and closes with a `SocketTimeoutException` (`WPP on TCP connection failed to read`, exactly as round 4). So `ddf8b198` is **harmless but inert on this build** — holding the channel open changes nothing the phone does and does not remove that phone-side error line; whether the phone pings on other Gearhead versions is unmeasured. Projection unaffected on every session. **R4 not run** (optional 2nd build, deferred as in round 4). Ship `native-wifi-version-exchange` **OFF** by default (unchanged); `a62cc22b` clears round 4's exchange-completion blocker, the cross-transport cached-endpoint case is handled by the withhold-text tail (documentation, not mechanism) and is unreachable with the flag off. `settings.xml` restored byte-identical, SoftAP down, candidate left installed. `SoftApInfo` 5765 MHz, SSID `Navegadortz2`, BSSID `00:27:15:43:06:6a`. Phone forgotten before R1 and again before R2. Results: `wpp-over-tcp-round5-results.md`.<br><br>Round 4 outcome, for reference: Candidate `fork/fix/wpp-over-tcp` @ `13a43a6d` (4 commits on `2f07eeec`), APK md5 `6fbff645b46e0bcf4616310950a21518`, **982/0**. **R0 PASS. R1 PARTIAL**: `WppTcpServer` now completes a mutual TLS handshake on every dial (`TLSv1.2, TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256`) — the round-2/3 blocker is gone — and a projection session forms and runs stable at 50 fps; but on the hotspot the phone is already on-network so it never sends `WifiInfoRequest` (Type 2), it sends `7` then `6` and stops, and `WppHandshakeSession.onAwaitInfoRequest` has no exit on `Type 6` / `TcpSessionUp`, so `handshake complete; projection session is up` is never logged and the phone prints a spurious `WPP on TCP connection failed to read` — neither fatal, projection is already up on the same socket. **R2 PASS (the feature working)**: hotspot reconnect forms a full session with `NativeAA: Connection accepted from` = 0 (no Bluetooth handshake) in ~5–7 s, twice, no loop, no brick — round 3's R3 infinite-loop is gone. **R3 connect PASS / reconnect FAIL (contaminated)**: fresh WiFi Direct connect is clean (endpoint withheld, no fresh poisoning); the reconnect looped and formed no session, but from a WPP-TCP config the phone cached during this round's own R1/R2 hotspot runs (Gearhead never restarted), not from `13a43a6d` — needs a clean-phone re-run. **R4 not run** (optional 2nd build). Ship the branch with `native-wifi-version-exchange` **off** by default (unchanged); before it can go on, `WppHandshakeSession` needs a hotspot-completion path and the cross-transport cached-endpoint case needs a decision. Results: `wpp-over-tcp-round4-results.md`. Round 3 results: `wpp-over-tcp-round3-results.md`. |
| `bring-up-status-pill` | **QUEUED.** Round 1, never run on hardware. | The home-screen pill that read "Android Auto is starting…" only during an auto-connect attempt is now up whenever the connection stack is armed, with a second line naming the bring-up step. Steps come from a new `ConnectionStageTracker` that about 25 call sites report into, each beside an `AppLog` line that already existed, and a pure `ConnectionStagePolicy` stops a repeated WiFi Direct callback dragging the display backwards. Candidate `fork/feat/bring-up-status-pill-and-poke-readiness` @ **`5aeb1a75`**, two commits on `main` `f5c2e986`; the lower commit `4e409bca` is a separate Native AA fix (the wake poke stops when the Android Auto listener is closed) and is not what the round tests. Gate **1609 tests / 0 failures**. No baseline APK: every condition is stated over `MainActivity: status pill step:`, a line that does not exist in `main`, and R4 is a settings-only discriminator instead. History was rewritten once, `46dbfd7d` no longer resolves. Brief: `bring-up-status-pill-round1-brief.md`. |
